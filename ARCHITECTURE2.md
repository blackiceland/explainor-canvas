🏛️ Обновлённая архитектура системы генерации анимированного технического контента (Explainor Studio)

📋 Оглавление
Обзор системы
Принципы проектирования
Эволюция архитектуры (4 фазы)
Текущая фаза: Component Library (v0 для себя)
Структура проекта
Ключевые компоненты и слои
IR и версионирование
Правила разработки
Тестирование и детерминизм

---

🎯 Обзор системы

Бизнес-цель
Платформа для детерминированной генерации анимированного технического контента (архитектурные схемы, графы систем, потоки запросов, визуализации алгоритмов) для YouTube-роликов, курсов и документации.

Целевая аудитория v1
В первую очередь авторы технического контента: dev-ютуберы, авторы курсов, техписатели, DevRel-команды. Для них ценность — быстро, предсказуемо и «на уровне инженерного смысла», без After Effects.

Value Proposition
«Semantic IR/JSON → красивое анимированное видео».

Быстрее AE: минуты вместо часов.
Проще Manim/Motion Canvas: декларативный сценарий вместо ручной покадровки.
Красивее PowerPoint/diagrams-tools: полноценная motion-анимация.

Ключевая идея продукта
Пользователь управляет смыслом и порядком действий. Система сама строит анимацию через библиотеку семантических операторов. Рендер — чистая детерминированная функция IR → видео.

---

🧭 Принципы проектирования

1. От простого к сложному
   Каждая фаза даёт рабочий продукт. Фичи добавляются только из реальных задач и сцен.

2. Снизу вверх и «реальные сцены сначала»
   Сначала компоненты и сцены под твои видео, затем обобщение в операторы и IR.

3. Детерминизм превыше всего
   Одинаковый IR + окружение → одинаковое видео. Это основа кэша, воспроизводимости и SaaS-рендера.

4. Семантика отделена от геометрии
   IR хранит смысл (что есть в сцене). Геометрия и координаты — вычисляемый слой layout, который можно менять независимо.

5. Операторы выше шотов
   Пользователь и JSON оперируют семантическими операторами. Шоты — внутренний механизм их исполнения.

6. Композиция над наследованием
   Компоненты не завязаны на конкретный движок. Никакого доменного наследования от Motion Canvas Node.

7. 70/30 по пространству
   Сначала 2D-сцены. 2.5D/изометрия — для акцентов, через отдельный рендерер и стиль.

---

🚀 Эволюция архитектуры

Phase 1: Component Library (Недели 1-4) 🟢 ← МЫ ЗДЕСЬ
Цель: библиотека переиспользуемых компонентов и ручные сцены для твоих роликов.

Что делаем
Пишем компоненты как доменные классы TypeScript.
Сцены собираем вручную.
Координаты задаём явно.
Рендер локальный через Motion Canvas UI.

Результат
15–20 базовых компонентов.
5–7 готовых сцен.
Чёткое понимание, какие операторы нужны.

Ключевое решение фазы
Компонент — это доменная сущность + внутренний view-object, а не наследник движка.

Пример (текущая реализация render-plane v0 для себя)

```ts
export abstract class AnimatedComponent {
  protected theme: Theme | null = null

  public mount(ctx: RenderContext): void {
    this.theme = ctx.theme
    this.onMount(ctx)
  }

  protected abstract onMount(ctx: RenderContext): void

  protected getTiming(duration?: number): number {
    if (duration !== undefined) {
      return duration
    }
    if (this.theme === null) {
      throw new Error('Theme is not initialized. Call mount(ctx) before using timing.')
    }
    return this.theme.timing.medium
  }

  abstract appear(duration?: number): Promise<void>
}
```

Внутри `onMount` компонент создаёт реальные ноды Motion Canvas через `RenderContext`, но наружу торчит только через наш контракт (`mount/appear`) и не раскрывает детали движка.

---

Phase 2: Scene Composition (Недели 5-8)
Цель: композиция сцен из шотов, но через слой семантических операторов.

Что добавляем
Low-level Shot-архитектура остаётся, но вводится слой Operators.
TimelinePlayer проигрывает шоты, которые порождаются операторами.

Результат
Сцена не монолит, а семантический план.
Шоты переиспользуются внутри операторов.
JSON в следующей фазе станет коротким и понятным.

Структура

```ts
export interface Operator {
  toShots(ctx: OperatorContext): Shot[]
}

export abstract class Shot {
  abstract execute(view: View2D): Promise<void>
}
```

Оператор `RevealGraph`, например, внутри создаст пакет шотов: камера, появление узлов, появление рёбер, подсветка.

---

Phase 3: IR-Driven (Недели 9-16)
Цель: сцены без кода, из декларативного IR.

Что добавляем
Semantic Scene IR — что есть в сцене и какие операторы применяются.
OperatorFactory и ElementFactory.
GenericScene исполняет IR.
Backend API (Spring Boot) хранит/версионирует IR.

Результат
IR/JSON → видео без .tsx.
Устойчивые миграции версий.
Backend начинает быть control plane.

Пример IR (семантика отдельно от операторов)

```json
{
  "version": "1.0",
  "sceneId": "kafka-producer-consumer",
  "renderMode": "2d",
  "entities": {
    "nodes": [
      { "id": "user", "kind": "USER", "label": "User" },
      { "id": "gateway", "kind": "SERVICE", "label": "Gateway" },
      { "id": "topic", "kind": "KAFKA_TOPIC", "label": "events" }
    ],
    "edges": [
      { "from": "user", "to": "gateway" },
      { "from": "gateway", "to": "topic" }
    ]
  },
  "operators": [
    { "type": "INTRODUCE_GRAPH", "target": "entities", "duration": 1.2 },
    { "type": "ZOOM_TO_NODE", "nodeId": "topic", "duration": 0.8 },
    { "type": "HIGHLIGHT_PATH", "path": ["user", "gateway", "topic"], "duration": 1.0 }
  ],
  "style": { "themeId": "default-light" }
}
```

GenericScene

```ts
export default makeScene2D(function* (view) {
  const ir = yield fetchScene(sceneId)
  const layout = yield computeLayout(ir)

  const ctx = new RenderContext(view, layout, ir.style)

  const graph = ElementFactory.createEntities(ir.entities, ctx)

  for (const opDef of ir.operators) {
    const op = OperatorFactory.create(opDef, graph, ctx)
    const shots = op.toShots(ctx)
    yield* TimelinePlayer.play(shots, view)
  }
})
```

---

Phase 4: Production SaaS (Недели 17-24+)
Цель: облачная платформа с автолейаутом, рендер-фермой и UI.

Что добавляем
Layout Engine как отдельный вычисляемый слой (ELKjs + свои правила).
Render Farm: очередь задач (Redis), N workers, хранение MP4 (S3/MinIO).
Scene Cache по hash(semantic IR + layout version + audio + theme).
Web UI: проекты, сцены, предпросмотр, чат-режиссёр.
Audio Sync: фразовые маркеры и синхронизация операторов с речью.
Themes и брендирование.

Результат
Стабильный SaaS.
IR без координат, координаты — результат Layout Engine.
Масштабируемый рендер и кэш.

---

📁 Структура проекта

	canvas/
	├── src/main/java/com/dev/canvas/
	│   ├── domain/
	│   │   ├── ir/
	│   │   ├── versioning/
	│   │   ├── layout/
	│   │   ├── render/
	│   │   └── audio/
	│   ├── application/
	│   │   ├── usecase/
	│   │   ├── controller/
	│   │   └── assembler/
	│   └── infrastructure/
	│       ├── db/redis/s3/queue
	│       └── adapters/tts
	│
	├── render-plane/        (концептуально)
	│   ├── src/
	│   │   ├── components/
	│   │   ├── operators/
	│   │   ├── shots/
	│   │   ├── ir/
	│   │   ├── layout/
	│   │   ├── renderers/
	│   │   └── cli/
	│   └── package.json
	│
	├── motion-canvas/       (фактическое расположение render-plane v0 для себя)
	│   ├── src/
	│   │   ├── core/        (Theme, RenderContext, AnimatedComponent, примитивы)
	│   │   ├── scenes/      (ручные сцены для роликов)
	│   │   └── services/
	│   └── package.json
	│
	├── ARCHITECTURE.md
	├── ROADMAP.md
	└── docker-compose.yml

---

🧩 Ключевые компоненты и слои

1. Semantic IR Core
   Доменные сущности сцены (nodes, edges, text blocks, code blocks, camera anchors) и доменные типы операторов. Никакой геометрии здесь нет.

2. Layout Layer
   На входе semantic IR. На выходе computed layout: координаты, размеры, кривые рёбер, bbox-ы, camera path hints. Layout версии должен быть частью хэша кэша.

```ts
export interface ComputedLayout {
  nodes: Record<string, { x: number, y: number, w: number, h: number }>
  edges: Record<string, { path: [number, number][] }>
  camera: Record<string, { x: number, y: number, zoom: number }>
}
```

3. Render Context
   Единая точка доступов компонентов к рендеру. В v0 он знает только про корневой `Node` и `Theme` (локальный рендер для себя). В следующих фазах сюда же приедет `ComputedLayout`.

```ts
export class RenderContext {
  constructor(
    public view: View2D,      // фактически сейчас: Node
    public theme: Theme       // в будущем: + layout: ComputedLayout
  ) {}

  createRect(spec: RectSpec): any {}
  createText(spec: TextSpec): any {}
  createLine(spec: LineSpec): any {}
  createGroup(spec: GroupSpec): any {}
}
```

4. AnimatedComponent (доменные компоненты)
   Компонент создаёт свои view-объекты через RenderContext и управляет ими через сигналы/анимации. Базовый класс берёт на себя инициализацию `Theme` и даёт хелпер `getTiming`, чтобы убрать хардкоды длительностей из компонентов. Снаружи его анимации — обещание для операторов.

5. Operators → Shots
   Оператор реализует смысловой эффект и компилируется в low-level shots. Shots исполняются TimelinePlayer-ом.

6. Scheduler (deterministic)
   Решает временные отношения: after/with/stagger/duringSpeech. На него не влияет AI. Он получает сцен-план и аудио-маркеры, возвращает конкретные тайминги.

7. AI Director (позже)
   Генерирует Operator-план из текста пользователя. Обязан выдавать валидный semantic IR и operator list. Никакого рендера «на лету».

---

📜 IR и версионирование

IR — публичный контракт. С первой версии:

* `version` — семантическая версия IR.
* миграции «на чтении» в backend и в render-plane.
* golden-тесты для каждой версии операторов.

Смена версии IR никогда не ломает старые проекты, только обновляет через миграцию.

---

📜 Правила разработки

Основной набор правил у тебя верный. Добавляю два системных:

Правило «движок-независимый домен».
Доменный компонент не наследуется от Motion Canvas Node. Только композиция через RenderContext.

Правило «семантика отдельно от координат».
В IR нельзя хранить computed-координаты, кроме ручного режима v0. Как только включён layout-алгоритм — координаты живут в ComputedLayout.

---

🧪 Тестирование и детерминизм

Юнит-тесты: компоненты и операторы в isolation (мок-RenderContext).
Golden frames: IR → рендер → PNG/MP4 сравнение с эталоном.

Детерминизм обеспечивается не только IR, но и окружением:

* фиксированные версии шрифтов, движка и ffmpeg;
* рендер в контейнере;
* seeded RNG в любом месте, где «дрожание» или случайность.

---

🚀 Заключение

Обновлённая архитектура сохраняет твою исходную фазность и простоту старта, но делает системные границы реальными: домен не прошит в Motion Canvas, IR не превращается в список низкоуровневых шотов, layout вычисляется отдельно, а планирование времени отделено от AI. Это даёт тебе быстрый личный инструмент уже в Phase 1 и нормальную траекторию к коммерческому SaaS без переписывания ядра.
