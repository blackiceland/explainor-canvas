import {buildDuplicationScene} from './_duplicationShared';

// Herbarium / matcha. Deep forest ground, mint + spring-leaf accents
// holding hands at the cool-green corner of the wheel, with a warm
// cream PUNCH that breaks the green plane just enough.
export default buildDuplicationScene({
    BG:     '#0F1F18', // deep forest
    INK:    '#E5F0E8', // cool minty white
    KEY:    '#A2D9C0', // mint        — keywords
    DOMAIN: '#BFD9A2', // spring leaf — function defs + strings
    PUNCH:  '#F8E0A0', // cream       — italic "bad."
    HERO:   '#A2D9C0', // mint for frame + hero
    QUIET:  '#6F7B73',
});
