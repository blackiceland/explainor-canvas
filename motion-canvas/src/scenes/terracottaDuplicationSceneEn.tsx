import {buildDuplicationScene} from './_duplicationShared';

// Sun-baked clay. Deep clay ground, terracotta + ochre accents
// walking the warm-orange lane, rose PUNCH for the only cool moment
// (the italic "bad." pulls the eye away from the warm bloc).
export default buildDuplicationScene({
    BG:     '#22120A', // deep clay
    INK:    '#F2E6DE', // warm clay-white
    KEY:    '#F0AA88', // terracotta  — keywords
    DOMAIN: '#F0CE88', // ochre       — function defs + strings
    PUNCH:  '#F0A8C0', // rose        — italic "bad." (cool tension)
    HERO:   '#F0AA88', // terracotta for frame + hero
    QUIET:  '#7B6E60',
});
