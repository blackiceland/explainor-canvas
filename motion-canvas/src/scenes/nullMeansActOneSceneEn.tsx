import {
  blur,
  brightness,
  contrast,
  Gradient,
  Img,
  Line,
  makeScene2D,
  Node,
  Rect,
  saturate,
  sepia,
  Txt,
} from '@motion-canvas/2d';
import {
  all,
  chain,
  createRef,
  createSignal,
  easeInCubic,
  easeInOutCubic,
  easeInOutSine,
  easeOutCubic,
  easeOutSine,
  SimpleSignal,
  Vector2,
  waitFor,
} from '@motion-canvas/core';
import {applyBackground} from '../core/utils';
import {Colors, Fonts, Screen} from '../core/theme';
import {Canon} from '../core/code/model/paletteCanon';

// ═══════════════════════════════════════════════════════════════════════
// АКТ 1 «YOUR NULL MEANS TOO MUCH» — ОДНОЙ СЦЕНОЙ.
//
// Драматургия: 1965 → ПРОЯВКА (свет приходит в кадр, год выгорает, из тепла
// набирает плотность снимок) → лист ALGOL заезжает справа КРУПНО и заявляет
// язык (на этой крупности читается и заголовок, и байлайн «C. A. R. Hoare») →
// на “Forty-four years later” лист уходит, и его колонку занимает приговор →
// цитату гасит тьма, в луче остаётся один человек → снимок уходит в темноту,
// и из неё всплывает неказистая стр. 9 Record Handling: «the mistake itself —
// far less so» → маркер по объявлению → печатная строка становится
// нашим кодом НА ТОМ ЖЕ МЕСТЕ (шаг литеры 18.05 × RH_READ_S = CODE_ADV,
// замер _rh9_rows.mjs) → столбец + карточка (строка N ↔ клетка N по высоте)
// → печать создания с ОБРЫВАМИ: father-обрыв → null, sibling-обрыв → null,
// K → горизонтальная стрелка в видимую вторую карточку → МОРФ: карточка
// становится его записью 1908 на стр. 11 (клетки тянутся на его ящик,
// краска темнеет до чернил, значения передают себя рукописи) → её null'ы →
// отъезд: вся семья его рукой → стр. 9, две цитаты-печати → признание
// «so easy to implement» → полоса → шторки.
//
// ⚠️ Порядок «карточка учит → чертёж опознаётся (морфом) → страница
//    подтверждает». Возврат на бумагу ОДИН, после конца графита.
//    Ничего не вводить дважды в разном виде; термин после вещи, не до.
// ⚠️ Текстовых тезисов в акте нет: их произносит озвучка.
// ⚠️ Даты: ALGOL — июнь 1966, Record Handling — сентябрь 1966. 1965 — год
//    работы над языком; подписывать этим годом сами документы нельзя.
// ═══════════════════════════════════════════════════════════════════════

const BG_SRC = '/hoare/hoare-background-3200x1350.jpg';
const CUT_SRC = '/hoare/hoare-cutout-3200x1350.png';
const IMG_W = 3200;
const IMG_H = 1350;
const FIG_CX = 1593;
const PHOTO_S = 0.88;
const toLayer = (imgX: number) => imgX - IMG_W / 2;
const FIG_SCREEN_X = 0.32 * Screen.width - Screen.width / 2;
const PAN_X0 = Math.round(FIG_SCREEN_X - toLayer(FIG_CX) * PHOTO_S);

const SERIF = 'Newsreader, serif';
const MONO = Fonts.code;

// ── 1965 ────────────────────────────────────────────────────────────────
// ⚠️ Год НЕ улетает и НЕ гаснет отдельно: его стирает раскрывающееся фото.
const YEAR = '1965';
const YEAR_FS = 110;
const YEAR_ADV = YEAR_FS * 0.6;
const YEAR_BEIGE = 'rgba(232, 207, 174, 0.96)';
const YEAR_KEY = 0.13;

// ⚠️ Стык 1965→фото = ПРОЯВКА (щель-затвор отменена автором). В кадр
// приходит свет: год выгорает в нём, а из света набирает плотность снимок —
// сначала тёплое латентное пятно (сепия = ровно тот тёплый тон, которым
// набран сам год), потом тепло уходит и остаётся серебро. Цвет идёт
// НЕПРЕРЫВНО: беж → сепия → ч/б, резкого стыка нет нигде.
// ⚠️ Никакого окна-раскрытия и его «передержки»: именно этот прямоугольник
// висел серой полосой за курсором всё время печати.
// ⚠️ Пауза между «год набран» и «пришёл свет» = 0.87с, и она обязана остаться
// такой: из неё складывается преролл 1.89с, а на нём стоит весь монтаж после.
// Внутри — ровный такт курсора: 4 полутакта по 0.2175 (2.3 Гц), начинается с
// погашенного и заканчивается горящим.
const CARET_HALF = 0.2175;
const CARET_STEPS = 4;
const YEAR_BURN = 'rgba(255, 249, 238, 1)';
const DEV_LIGHT = 0.7;               // свет приходит, год выгорает
const DEV_SHARP = 1.25;              // плотность и резкость отпечатка
const DEV_GRADE = 2.1;               // уход тепла: сепия → серебро
const DEV_BLUR = 13;
// ⚠️ Наезд идёт ЧЕРЕЗ ВЕСЬ настенный блок (PUSH_D считается из его частей):
// раньше он кончался за 5с и дальше кадр стоял мёртво.
const PUSH_K = 1.085;
const PUSH_AT_Y = -140;              // точка наезда: лицо чуть выше центра
// ⚠️ Камера не только наезжает, но и ИДЁТ СПРАВА НАЛЕВО (правка автора):
// одной разницы масштабов для параллакса мало, его выдаёт боковой ход.
// Запас справа на конце ~90px — дальше кромка снимка войдёт в кадр.
const PAN_DX = 140;
const PUSH_X1 = FIG_SCREEN_X + (PAN_X0 - FIG_SCREEN_X) * PUSH_K - PAN_DX;
const PUSH_Y1 = PUSH_AT_Y * (1 - PUSH_K);
// ⚠️ ПАРАЛЛАКС: вырезка Хоара получает СВОЙ ход — уезжает влево заметно
// быстрее стены и растёт быстрее неё. Масштабируется от линии стола (низ
// выреза), иначе срез фигуры выползет на столешницу. Снос ограничен −52:
// дальше из-за его плеча вылезет собственная тень, оставшаяся на стене.
const HOARE_K = 1.06;
const HOARE_BASE_Y = 381;
const HOARE_DX = -52;
// ⚠️ Отпечатку дан только вдох: страница обязана прийти на словах
// “designing a new language”, а не через полторы секунды после них.
const SETTLE = 0.15;

// ── краска на стене ─────────────────────────────────────────────────────
// ⚠️⚠️ ОДИН СЛОТ НА ПРАВОЙ СТЕНЕ — несущая идея акта. В нём по очереди живут
// ТРИ вещи и все входят/выходят одним путём, справа: белый документ (язык) →
// приговор → жёлтый документ (ошибка). Ничего не растёт на весь кадр, ничего
// не гасится, новых направлений движения нет.
// ⚠️ Цитата стоит СПРАВА, ПО ЦЕНТРУ СТЕНЫ (правка автора): ось +480 экранных,
// центр блока −95 (стена идёт от верха кадра до столешницы на +350, её
// середина −95). Замеры: ось идёт 488 → 477 за жизнь цитаты, снос параллакса
// 11px. QUOTE_X/Y пересчитаны из этих величин через масштаб снимка в середине
// жизни цитаты (s 0.9513, photo −471.8 / +11.34).
// ⚠️ Кегль снижался дважды. 62 → 58 — по геометрии: лист режется правой
// кромкой кадра, но обязан НАКРЫВАТЬ надпись целиком, иначе не сможет её
// открыть, а строка 680px между левой кромкой листа (228) и безопасным полем
// у края не помещалась. 58 → 52 — правка автора «на 10% меньше»; весь блок
// ужат пропорционально (PITCH и подпись тоже). Строка теперь 572px:
// лист 228…972, цитата 314…886 — запас 86/86 внутри листа и 74px до кромки.
// QUOTE_Y пересчитан: центр блока сместился с +27 на +23 в локальных единицах.
const QUOTE_X = 2600;
const QUOTE_Y = 540;
const QUOTE_FS = 52;
const QUOTE_PITCH = 70;
const QUOTE_FILL = 'rgba(44, 44, 48, 0.85)';
const QUOTE_SHADOW = 'rgba(0, 0, 0, 0.5)';
const ATTRIB_FS = 22.5;
const ATTRIB_DY = 63;
const ATTRIB_FILL = 'rgba(44, 44, 48, 0.58)';
// ⚠️ Цитата НЕ проявляется отдельным ходом: она уже написана на стене и
// выходит из-под уезжающего листа. Ни фейда, ни лесенки строк, ни масштаба.
const QUOTE_HOLD = 3.30;             // ⚠️ VO — “The phrase became famous”, чистый неподвижный hold
// ⚠️ ЗАТЕМНЕНИЕ ФОНА ОТМЕНЕНО ЦЕЛИКОМ (правка автора: «фон не нужно гасить до
// черноты… если на стене текст читается и так — вообще ничего с фоном не
// делать»). Тёмная вуаль, лужа света и блюр под цитату удалены — они и делали
// «дешёвую рекламную перебивку». Цитату гасит не свет, а сам документ,
// который наезжает на неё кромкой (см. GROW).

// ── страница ALGOL: главный экспонат акта ───────────────────────────────
// ⚠️ Лист живёт НЕ на стене, а на плоскости фильма (правка автора): он
// приезжает справа крупным и наклонённым и стоит НЕПОДВИЖНО, пока снимок за
// ним дрейфует наездом, — два плана дают глубину, которой листок, налепленный
// на стену, не давал. Его работа — заявить язык: на этой крупности читаются и
// заголовок «A Contribution to the Development of ALGOL», и байлайн
// «C. A. R. Hoare», то есть ОДИН объект отрабатывает сразу и “designing a new
// language”, и “his name is Tony Hoare”.
const ALGOL_SRC = '/hoare/algol-sheet-1181x1594.jpg';
const GRAIN_SRC = '/hoare/grain-1920x1080.png';
const GRAIN_OP = 0.5;
const SH_W = 1181;
const SH_H = 1594;
// ⚠️⚠️ ГЛАВНОЕ РЕШЕНИЕ АКТА (правка автора): лист ALGOL физически проводит
// зрителя через всю сцену и САМ открывает под собой жёлтую страницу. Он не
// декоративная карточка и не уходит из кадра: въезжает справа → держит правую
// половину → расходится с Хоаром, освобождая центр под цитату → наезжает на
// цитату и стирает её собой → заполняет кадр → уезжает вверх-влево, открывая
// Record Handling. Вся сцена собрана на ТРЁХ действиях: лист входит справа,
// лист становится сценой, верхний лист уходит влево и открывает следующий.
// Никаких отдельных заставок, затемнений и новых направлений движения.
// ⚠️ Документы уменьшены на 20% (правка автора): 930 → 744, то есть 39%
// ширины кадра. Ось листа сведена с осью цитаты — это ОДИН И ТОТ ЖЕ слот, и
// лист обязан накрывать надпись целиком (лист +108…+852, цитата +140…+820,
// запас по 32px с каждой стороны). Иначе он не сможет её открыть.
// ⚠️ Ширина 820, а не 744: крой правого края берётся ШИРИНОЙ листа, а не его
// сдвигом вправо (правка автора «не нравится, что цитата ужалась вправо»).
// Арифметика, из-за которой это единственный выход: лист обязан накрывать
// надпись целиком (иначе не сможет её открыть) и одновременно уходить за
// правую кромку. Для цитаты на её месте (194…766) это даёт 2·полуширина > 796,
// то есть ширину от 800. При 820 лист 155…975: за кадр уходит 15px справа, а
// по высоте 1107 против 1080 — верх и низ тоже срезаны, и «вставленная
// карточка» пропадает окончательно. Видимая доля 42% против прежних 38%.
const PAGE_W = 820;
const PAGE_SCALE = PAGE_W / SH_W;
const PAGE_X = 565;
const PAGE_Y = 0;
const PAGE_TILT = -1.2;              // после остановки; на въезде — PAGE_TILT_IN
const PAGE_TILT_IN = -4;
const PAGE_LIFT = 0.93;              // белый бумаги садится в тон отпечатка
// ⚠️ СПОТЛАЙТ НА ЗАГОЛОВОК СТАТЬИ (правка автора). Свет ложится широкой
// плоской лужей на строку «A Contribution to the Development of ALGOL»
// (замер по кадру: заголовок на экране y ≈ −360 при масштабе 0.694, значит в
// координатах листа −519), остальное поле бумаги мягко уходит. Затемнение
// СЛАБОЕ (макс. 0.30 в углах листа) и только ПО БУМАГЕ: пятно лежит в окне
// SH_W×SH_H с clip, иначе тень выползла бы на фотографию.
// ⚠️⚠️ ПРИЧИНА, ПО КОТОРОЙ ТРИ ПРЕДЫДУЩИЕ ВЕРСИИ БЫЛО НЕ ВИДНО: я строил
// РАДИАЛЬНОЕ пятно. На листе 1181×1594 радиальная лужа с центром на строке
// заголовка неизбежно превращается либо в полосу с границами (если сжать),
// либо в градиент по всей странице (если растянуть) — локального выделения
// одной строки радиус дать не может. Плюс положительная прибавка упиралась
// в потолок: бумага и так 237, headroom 18 уровней.
// ⚠️ Референс автора — как в старых фильмах подсвечивали глаза: ЩЕЛЬ СВЕТА
// поперёк кадра. Это не пятно, а ЛИНЕЙНАЯ функция от одной координаты: весь
// лист уходит в тень, и поперёк него, во всю ширину, идёт узкая светлая
// полоса по строке заголовка. Мягкие края даёт растушёвка полосы, а не
// падение по радиусу.
// ⚠️⚠️ ДОБАВЛЯТЬ СВЕТ НА ЭТУ БУМАГУ НЕЛЬЗЯ — и это не вопрос вкуса.
// Скан идёт 255/255/255, аддитивный слой упирается в потолок и на бумаге
// не даёт НИЧЕГО (замер: 237 → 237, коэффициент 1.0000), зато на краске
// заголовка поднимает чёрное с 1 до 21 — контраст падает с 234 до 215.
// То есть «свет» ЗАМЫЛИВАЕТ ровно ту строку, которую выделяет. Аддитивный
// слой удалён; щель делается ТОЛЬКО тенью вокруг, как в кино и делают.
// ⚠️ Полоса ДОЛЖНА БЫТЬ УЗКОЙ: прошлая (145px ядра) накрывала строку Кнута,
// заголовок, оба авторских блока и начало аннотации — шесть блоков сразу,
// акцента на названии не было. Ядро ±70 локальных = 97px экранно ≈ две
// высоты строки заголовка.
// ⚠️ У луча обязаны быть КОНЦЫ. Одна вертикальная щель без горизонтального
// спада читается леттербоксом. Спад по горизонтали идёт ОТДЕЛЬНЫМ слоем:
// свести обе оси в один радиальный градиент нельзя — отношение R1/R0 общее
// для осей, и узкая полоса автоматически даёт мгновенный вертикальный спад.
const BAND_Y = -519;                 // ось полосы = строка заголовка
const BAND_HALF = 70;
const BAND_FEATHER = 160;            // растушёвка 160 лок. = 111px экранно
// ⚠️ Затемнение усилено по правке автора. Глубину ограничивает ФИЗИКА:
// собственная тень листа на стене шла 158 при стене 169, и уже при 0.30×0.20
// бумага уходила на 133 — темнее своей же тени, затенение читалось
// перевёрнутым. Поэтому вместе с бумагой углубляется и ТЕНЬ ЛИСТА НА СТЕНЕ
// (shadowBoost, привязан к той же прозрачности): свет гаснет — тень растёт.
const BAND_DARK = 0.38;
const BAND_EDGE = 0.18;              // спад к левой и правой кромке листа
const BAND_EDGE_X = 330;             // до этой полуширины луч ровный
const SPOT_IN = 0.6;                 // ⚠️ VO — “designing a new language”
const SPOT_HOLD = 2.6;               // ⚠️ VO — держит и байлайн «C. A. R. Hoare»
const SPOT_OUT = 0.7;                // снимается до “Forty-four years later…”
// ⚠️ Лист БЕЛЫЙ С САМОГО НАЧАЛА (правка автора). Ни тонировки, ни тени, ни
// приглушённой прозрачности — любое гашение читается как «опустил opacity».
// ⚠️ Въезд 0.7с, спокойный ease-out, без пружины и перелёта через точку:
// «редактор положил перед нами архивный документ», а не прилетела карточка.
const PAGE_DX = 320;
const PAGE_IN = 0.7;                 // ⚠️ VO — “designing a new language”
const PAGE_HOLD = 7.35;              // ⚠️ VO — “His name is Tony Hoare” + “Forty-four years later…”
// ⚠️ Лист уезжает ЦЕЛИКОМ ЗА КАДР и БЕЗ фейда, и его левая кромка ОТКРЫВАЕТ
// цитату (правка автора: «после свапа первой страницы цитата должна быть уже
// на стене»). Она написана на стене заранее и просто выходит из-под бумаги —
// никакого отдельного проявления. Ход 900 = 960 (край кадра) + 372
// (полуширина листа) − 480 (ось слота). Фейда быть не может: полупрозрачный
// лист над надписью тут же даёт двойную экспозицию.
const PAGE_OUT = 1.1;
const PAGE_OUT_DX = 900;

// ── мост: из комнаты на стол ────────────────────────────────────────────
// ⚠️ Диагноз автора: «перехода к жёлтым страницам нет никакого, жёлтая
// страница выглядит осиротевшей». Так и было: стр. 9 — СЕРЕДИНА документа,
// на ней нет ни названия, ни автора, и зритель не знает, что за бумагу ему
// показывают. Мост восстановлен и сделан честно: документ представляет
// СОБСТВЕННЫЙ титульный лист (тот же скан, та же бумага, тот же стол,
// ── третий житель слота: титул Record Handling ──────────────────────────
// ⚠️ «Страница забирает весь кадр, слишком давлеет» — рост белого листа на
// весь экран ОТМЕНЁН. Ни один документ в комнате больше не превышает своей
// доли кадра. Жёлтый титул ВЕРНУЛСЯ и приходит в ТОТ ЖЕ слот тем же ходом
// справа, закрывая собой приговор: улика буквально занимает место фразы.
// ⚠️ Наезд в документ идёт скромный — 930 → 1304px ширины (×1.4), и ровно в
// ту кадрировку, которую ждёт вторая половина (RH_AT_REVEAL / RH_REVEAL_S).
// Комната при этом не гаснет: вокруг листа ПРОСТУПАЕТ ЕГО СОБСТВЕННЫЙ СТОЛ —
// тот же ассет, только без обрезки по кромке бумаги. Мы не меняем сцену, мы
// подходим к документу вплотную, и его контекст приходит вместе с ним.
// ⚠️ Сумма TITLE_IN + TITLE_HOLD + PUSH_IN + LIFT + LIFT_SETTLE = 6.1с.
const TITLE_W = 820;                 // ровно та же масса, что у листа ALGOL
const TITLE_TILT = -1.4;
const TITLE_TILT_IN = -4;
const TITLE_DX = 320;
// ⚠️ ЗАЕЗД И ПОДХОД — ОДИН НЕПРЕРЫВНЫЙ ХОД (правка автора: «лишний мув»).
// Лишним был не приход титула, а ОСТАНОВКА: он приезжал, вставал, стоял
// полторы секунды, и только потом мы к нему шли — три состояния подряд, из
// которых среднее жило 1.6с и тут же сносилось вместе с комнатой. Теперь
// документ входит справа, накрывает собой цитату и НЕ ТОРМОЗИТ: продолжает
// идти к нам, комната уступает, вокруг него проступает его стол.
// ⚠️ Пауза на чтение переехала ТУДА, ГДЕ ТИТУЛ ЧИТАЕТСЯ — на конечную
// крупность, а не в комнату, где он мелкий.
// ⚠️ «Бесконечно долго выползает» — дело было не только в длительности, а в
// ХАРАКТЕРЕ хода. Замер: лист ALGOL проходит 320px за 0.7с (457 px/с), титул
// шёл 885px — та же скорость, но в 2.8 раза дольше, плюс easeInOutSine делал
// вязким и старт, и финиш. Теперь 1.1с с easeOutCubic: решительная выкладка и
// короткий доводочный хвост, а не ползание. Освободившееся отдано чтению.
const TITLE_COME = 1.1;              // ⚠️ VO — “The mistake itself — far less so”
const TITLE_READ = 3.4;              // прочесть RECORD HANDLING · C. A. R. Hoare · 1966
const LIFT = 1.1;                    // титул снимают, под ним стр. 9
const LIFT_OUT = 1780;
const LIFT_SETTLE = 0.5;

// Наезд на Хоара живёт ровно столько, сколько длится настенный блок — от
// прихода света до момента, когда в слот приходит жёлтый титул.
const PUSH_D = DEV_GRADE + SETTLE + PAGE_IN + PAGE_HOLD + PAGE_OUT + QUOTE_HOLD;

// ── страницы Record Handling ────────────────────────────────────────────
const RH1_SRC = '/hoare/record-handling-p1-4800x2700.jpg';   // титул документа
const RH9_SRC = '/hoare/record-handling-p9-4800x2700.jpg';
const RH11_SRC = '/hoare/record-handling-p11-4800x2700.jpg';
const RH_W = 4800;
const RH_H = 2700;
const PAPER_W = 1672;
const PAPER_H = 2440;
const TILT = -0.45;
const RH_REVEAL_S = 0.78;
const RH_AT_REVEAL = {x: 836, y: 1180};
const RH_READ_S = 1.2;
const RH_PUSH = 1.1;
const AT_DECL = {x: 810, y: 1158};
const AT_PARTIAL = {x: 810, y: 1851};
const AT_NULL = {x: 810, y: 2011};

const HL_FILL = 'rgb(247, 179, 196)';
// ⚠️ объявление занимает ТРИ печатные строки — маркер идёт по всем трём
// (замеры _rh9_rows.mjs, координаты листа)
const HL_DECL_1 = {x0: 117, x1: 1133, cy: 1010, h: 46};
const HL_DECL_2 = {x0: 500, x1: 1395, cy: 1050, h: 46};
const HL_DECL_3 = {x0: 1246, x1: 1427, cy: 1089, h: 46};
const HL_PARTIAL = {x0: 120, x1: 849, cy: 1851, h: 46};
const HL_NULL_A = {x0: 1040, x1: 1428, cy: 2011, h: 45};
const HL_NULL_B = {x0: 120, x1: 266, cy: 2051, h: 43};
const HL_DUR = 0.55;
const HL_STEP = 0.16;
const READ_DECL = 1.1;               // ⚠️ VO
const HL_WRAP = 0.28;
const READ_PARTIAL = 1.6;            // ⚠️ VO
const READ_NULL = 1.5;               // ⚠️ VO

// ── схема Хоара, стр. 11 (замеры _rh11_measure.mjs, координаты листа) ───
const BOX_1908 = {x0: 989, x1: 1177, y0: 471, y1: 737};
// ⚠️ В записи 1908 пусты father и elder sibling, а youngest offspring —
// СТРЕЛКА. Наша графитовая карточка обязана повторять это один в один,
// иначе узнавание на его чертеже ломается.
const NULLS_1908 = [
  {x0: 1038, x1: 1110, cy: 608, h: 46},    // father
  {x0: 1038, x1: 1111, cy: 660, h: 46},    // elder sibling
];
const NULLS_REST = [
  {x0: 1335, x1: 1407, cy: 1122, h: 46},   // 1934 · elder sibling
  {x0: 1335, x1: 1407, cy: 1174, h: 46},   // 1934 · youngest offspring
  {x0: 403, x1: 500, cy: 1194, h: 46},     // 1937 · youngest offspring
  {x0: 732, x1: 806, cy: 1710, h: 46},     // 1964 · elder sibling
  {x0: 732, x1: 806, cy: 1762, h: 46},     // 1964 · youngest offspring
];
const DIAG_AT_WIDE = {x: 915, y: 1115};
const DIAG_S_WIDE = 0.71;
// ⚠️ DIAG_AT_BOX = ЦЕНТР записи 1908 — при пуш-кадровке она встаёт ровно в
// (0,0) экрана, и туда же морфится наша карточка.
const DIAG_AT_BOX = {x: 1083, y: 604};
const DIAG_S_BOX = 2.0;
// ⚠️ МОРФ «карточка → его запись» В ДВЕ ФАЗЫ (правка автора): (A) карточка
// СНАЧАЛА центрируется и растягивается на место ящика 1908 — страница ещё
// не видна, семья и код гаснут; (B) КРОССФЕЙД: страница проявляется, наша
// краска темнеет до чернил и уступает рукописи. Никакого морфа глифов.
const MORPH_A = 1.0;
const MORPH_B = 0.8;
const MORPH_INK = 'rgba(52, 47, 40, 0.85)';
// p11 откладывают тем же жестом, что лист ALGOL: уезжает влево-вниз с
// поворотом, под ним уже лежит стр. 9
const P11_ASIDE = 1.0;
const BOX_HL_HOLD = 1.6;             // ⚠️ VO
const DIAG_BACK = 1.3;
const REST_STEP = 0.22;
const DIAG_HOLD = 1.6;               // ⚠️ VO

// ── наш набор: палитра-канон ────────────────────────────────────────────
// ⚠️ По канону строчный идентификатор = ink, тип = лаванда, ключевое слово и
// число = синий. Поля записи — обычные идентификаторы (ink), НЕ param.
const CODE_FS = 36;
const CODE_ADV = CODE_FS * 0.6;
const KW = Canon.keyword;
const TY = Canon.type;
const IDENT = Canon.ink;
const PUN = Canon.punctuation;
const ROSE = Canon.methodCall;

// ── графит: код слева, карточка справа ──────────────────────────────────
// COL_X совпадает с экранным началом первой печатной строки, поэтому при
// перестроении в столбец шапка объявления по горизонтали НЕ едет.
const COL_X = -829;
const COL_Y0 = -228;
// ⚠️ 68, не 76: при 76 запись K и третья строка кода упирались в низ кадра
// (правка автора «уменьшить отступы между строк в record»)
const ROW_H = 68;
// ⚠️ Столбец сдвинут левее (правка автора). COL_X трогать НЕЛЬЗЯ — из него
// выведено рождение кода на машинописной строке; сдвиг живёт отдельной
// величиной и применяется только к КОЛОНОЧНЫМ позициям, то есть после
// перестроения.
const COL_DX = -70;
// ⚠️ Отступ между ЛОГИЧЕСКИМИ блоками отличается от межстрочного (правка
// автора): объявление record class — один блок (строки 0–6), объявление
// переменных `reference (person) T, J, K;` — второй (строка 7), строки
// создания — третий (MAKE_Y). Строки 1–5 сдвигать нельзя: по ним выровнены
// клетки карточки (cellY), поэтому добавка начинается только с 7-й.
const BLOCK_GAP = 30;
const colAt = (row: number, col: number) =>
  [COL_X + COL_DX + col * CODE_ADV, COL_Y0 + row * ROW_H + (row >= 7 ? BLOCK_GAP : 0)] as const;

const CARD_X = 330;
const CARD_W = 300;
const CARD_H = ROW_H * 5;
const cellY = (i: number) => COL_Y0 + ROW_H * (i + 1);   // клетка i ↔ строка i+1
const CARD_STROKE = 'rgba(244, 241, 235, 0.34)';
const CARD_LABEL_FS = 34;
const CARD_VAL_FS = 34;

// ⚠️ Вторая карточка ЦЕЛИКОМ В КАДРЕ — стрелка в закадровое «никуда» не
// читалась. Её первая клетка стоит РОВНО на высоте пятой клетки первой
// карточки, поэтому стрелка — чистая горизонталь без изломов.
const OTHER_X = 780;
const OTHER_Y = cellY(4) - ROW_H / 2 + CARD_H / 2;

// ⚠️ Третья запись (1964, младший потомок K) — та же рифма: её первая клетка
// на высоте ПЯТОЙ клетки K, стрелка снова чистая горизонталь (K смотрит
// влево-вниз). Чтобы семья поместилась, кадр честно отъезжает: graph
// масштабируется ЦЕЛИКОМ, ничего не гасится и не обрезается.
const K_CELL5_Y = OTHER_Y - CARD_H / 2 + ROW_H * 4.5;
const C3_X = 400;
const C3_Y = K_CELL5_Y - ROW_H / 2 + CARD_H / 2;
// ⚠️ ВЕСЬ ГРАФИТ НА 10% МЕЛЬЧЕ (правка автора: «уменьши код и схемы на 10
// процентов»). Масштаб нельзя ставить с самого начала: в §7 печатная строка
// становится нашим кодом НА ТОМ ЖЕ МЕСТЕ И ТОГО ЖЕ КЕГЛЯ — там graph обязан
// стоять на 1, иначе стык с машинописью разъедется (COL_X −829 выведен из
// screenOf при RH_READ_S). Поэтому мир ужимается ВМЕСТЕ с перестроением в
// столбец (§8), где всё и так едет.
// ⚠️ Камера графита (FAM_*) и страница схемы (rh11) множатся на тот же
// коэффициент — иначе морф карточки в запись 1908 промахнётся: цель считается
// как boxW·DIAG_S_BOX в ЛОКАЛЬНЫХ единицах work, а на экран выходит через
// GRAPH_S, и обе стороны обязаны сжаться одинаково.
const GRAPH_S = 0.9;
const FAM_S = 0.8 * GRAPH_S;
const FAM_POS = [-40 * GRAPH_S, -174 * GRAPH_S] as const;
const FAM_D = 1.0;
const FAM_HOLD = 1.6;                // ⚠️ VO — семья: два человека по ссылкам

const REFLOW = 1.1;
const CARD_IN = 0.7;
const FIELD_STEP = 0.75;             // ⚠️ VO — по одному полю
const ARROW_DRAW = 0.55;

// ⚠️ ТРИ строки создания каскадом: каждая ссылка тянет следующую строку —
// «чтобы записать одного человека, пришлось создать троих», и дыр всё
// больше (последняя строка кончается тремя null). Карточка появляется
// ПОСЛЕ закрытия своей скобки и заполняется СВОЕЙ строкой.
const MAKE_Y = 330;
const MAKE_PITCH = 52;
const MAKE_CHAR = 0.026;
// ⚠️ Обрывы печати: на father долгий (здесь сидел Хоар — писать нечего),
// на elder sibling короткий (та же беда, уже понятная), перед K — никакого.
const FATHER_STALL = 1.7;            // ⚠️ VO
const SIB_STALL = 0.9;               // ⚠️ VO
const K_HOLD = 0.9;                  // ⚠️ VO — единственная настоящая ссылка
const PAYOFF_HOLD = 2.0;             // ⚠️ VO — запись: две дырки, одна стрелка

const EASY_IN = 0.8;
const EASY_HOLD = 2.8;               // ⚠️ VO

// ── лента языков в финальной полосе (возврат по просьбе автора) ─────────
// ⚠️ БЕЗ барабанов (отвергнуты отдельно): пары «язык — конструкция»,
// фокус шагает по ленте, соседи приглушены.
const LANGS: {name: string; form: string}[] = [
  {name: 'Kotlin', form: 'T?'},
  {name: 'Swift', form: 'Optional<T>'},
  {name: 'TypeScript', form: 'T | null'},
  {name: 'C#', form: 'T?'},
  {name: 'Rust', form: 'Option<T>'},
  {name: 'Java', form: 'Optional<T>'},
];
const BELT_FS = 44;
const BELT_CHAR = BELT_FS * 0.6;
const BELT_INNER = 52;
const BELT_GAP = 200;
const BELT_NAME = 'rgba(232, 207, 174, 0.78)';
const BELT_FORM = 'rgba(244, 230, 200, 0.96)';
const BELT_DIM = 0.28;
const BELT_IN = 0.6;
const BELT_STEP = 0.5;
const BELT_READ = 0.6;               // ⚠️ VO — на каждый язык

const SQUEEZE_DUR = 1.4;
const BAND_H = 164;
const BAND_HOLD = 0.7;
const SHUT_H = 700;
const SHUT_DUR = 0.6;

export default makeScene2D(function* (view) {
  applyBackground(view);
  // ⚠️ Кэш узла режется рамкой вьюпорта (Node.worldSpaceCacheBBox), поэтому
  // блюр проявки размывался в прозрачность прямо по краю кадра и давал
  // тёмный кант. Запас отодвигает границу кэша за экран.
  view.cachePadding(90);

  yield Promise.all(
    [BG_SRC, CUT_SRC, ALGOL_SRC, GRAIN_SRC, RH1_SRC, RH9_SRC, RH11_SRC].map(
      src =>
        new Promise<void>(resolve => {
          const el = document.createElement('img');
          el.onload = () => resolve();
          el.onerror = () => resolve();
          el.src = src;
        }),
    ),
  );

  const frameH = createSignal(Screen.height);
  const stage = createRef<Rect>();
  view.add(<Rect ref={stage} width={Screen.width} height={() => frameH()} clip />);

  // ═══ 1. «1965» консолью ══════════════════════════════════════════════
  // ⚠️ Год и курсор — ОДИН узел, и он ляжет ПОВЕРХ проявляющегося снимка:
  // цифры гасит пришедший свет. За курсором не тянется ничего.
  const yearWrap = new Node({});
  const yearLeft = -(YEAR.length * YEAR_ADV) / 2;
  const yearTxt = new Txt({
    text: '',
    x: yearLeft,
    offset: [-1, 0],
    fontFamily: MONO,
    fontSize: YEAR_FS,
    fontWeight: 500,
    fill: YEAR_BEIGE,
  });
  const cursor = new Rect({
    x: yearLeft + YEAR_ADV * 0.5,
    offset: [-1, 0],
    width: YEAR_ADV * 0.72,
    height: YEAR_FS * 0.98,
    fill: YEAR_BEIGE,
    opacity: 0,
  });
  yearWrap.add(yearTxt);
  yearWrap.add(cursor);

  // ═══ 2. Снимок: проявка и наезд ══════════════════════════════════════
  // ⚠️ `room` — обёртка НАД снимком: наезд живёт на самом `photo` одним
  // длинным твином на весь блок, а расхождение к краям («снимок уходит на
  // 2.5% влево») обязано лечь поверх него отдельной величиной. Двумя твинами
  // одного position они бы дрались.
  const room = createRef<Node>();
  const photo = createRef<Node>();
  const bgLayer = createRef<Node>();
  const hoareLayer = createRef<Node>();
  // грейд проявки: 1 = тёплое латентное пятно, 0 = готовый отпечаток.
  // sepia на серебряном снимке даёт ровно тот беж, которым набран год, —
  // поэтому цвет перетекает, а не переключается.
  const devSoft = createSignal(1);
  const devWarm = createSignal(1);
  stage().add(<Node ref={room} />);
  room().add(
    <Node
      ref={photo}
      x={PAN_X0}
      scale={PHOTO_S}
      opacity={0}
      filters={[
        // ⚠️ sepia поднимает КРАСНЫЙ в 1.35 раза: при brightness > 1 он
        // упирался в 255 первым, и светлая стена уезжала в зелень. Пока
        // тепло на кадре, экспозиция чуть придержана — клиппинга нет.
        blur(() => DEV_BLUR * devSoft()),
        brightness(() => 1 - 0.1 * devWarm()),
        contrast(() => 1 - 0.36 * devSoft()),
        sepia(() => devWarm()),
        saturate(() => 1 + 0.35 * devWarm()),
      ]}
    />,
  );
  stage().add(yearWrap);
  photo().add(<Node ref={bgLayer} />);
  bgLayer().add(<Img src={BG_SRC} width={IMG_W} height={IMG_H} />);
  photo().add(<Node ref={hoareLayer} />);
  hoareLayer().add(<Img src={CUT_SRC} width={IMG_W} height={IMG_H} />);

  // ── краска на стене + её горизонтальный смаз ─────────────────────────
  const wallLine = (text: string, y: number, fs: number, fill: string, shadow: boolean) => (
    <Txt
      text={text}
      y={y}
      fontFamily={SERIF}
      fontSize={fs}
      fontWeight={500}
      letterSpacing={2}
      fill={fill}
      shadowColor={shadow ? QUOTE_SHADOW : 'rgba(0,0,0,0)'}
      shadowBlur={shadow ? 16 : 0}
      shadowOffset={shadow ? new Vector2(18, 13) : new Vector2(0, 0)}
    />
  );
  const quote = createRef<Node>();
  const attrib = createRef<Node>();
  // ⚠️ Строки — ОТДЕЛЬНЫЕ узлы: они проступают по очереди с задержкой в 4
  // кадра. Никакого scale на узле цитаты (был 1.03 → 1): «без увеличения,
  // вращения и размытого прилёта».
  const qLine1 = new Node({opacity: 0});
  const qLine2 = new Node({opacity: 0});
  bgLayer().add(<Node ref={quote} x={toLayer(QUOTE_X)} y={QUOTE_Y - IMG_H / 2} opacity={1} />);
  qLine1.add(wallLine('“MY BILLION-DOLLAR', -QUOTE_PITCH / 2, QUOTE_FS, QUOTE_FILL, true));
  qLine2.add(wallLine('MISTAKE.”', QUOTE_PITCH / 2, QUOTE_FS, QUOTE_FILL, true));
  quote().add(qLine1);
  quote().add(qLine2);
  quote().add(<Node ref={attrib} opacity={0} />);
  attrib().add(wallLine('TONY HOARE · 2009', QUOTE_PITCH / 2 + ATTRIB_DY, ATTRIB_FS, ATTRIB_FILL, false));

  // ── лист ALGOL ───────────────────────────────────────────────────────
  // ⚠️ Стопка есть у ОБОИХ листов и приходит ВМЕСТЕ со страницей (правка
  // автора): когда подлисты возникали только на зуме, это читалось как
  // «из-под страницы вдруг вылезла вторая».
  const makeAlgolSheet = () => {
    const n = new Node({});
    // ⚠️ Подлисты стопки притушены к тону бумаги (было 196/212): при жёстком
    // контрасте их кромки читались как края второй карточки, а не как глубина.
    n.add(new Rect({width: SH_W, height: SH_H, x: 10, y: 12, rotation: 0.9, fill: 'rgb(210, 206, 197)'}));
    n.add(new Rect({width: SH_W, height: SH_H, x: 5, y: 6, rotation: -0.45, fill: 'rgb(221, 217, 208)'}));
    n.add(
      new Rect({
        width: SH_W,
        height: SH_H,
        fill: 'rgb(232,228,219)',
        // ⚠️ Тень СЛАБАЯ И ШИРОКАЯ — контактная (правка автора). Резкая тень
        // отделяла документ от снимка и давала интерфейсную карточку. Экранно
        // при PAGE_SCALE 0.63: размытие ~95px, снос 4/6px, альфа 0.18.
        shadowColor: 'rgba(0,0,0,0.18)',
        shadowBlur: 150,
        shadowOffset: new Vector2(6, 10),
      }),
    );
    n.add(new Img({src: ALGOL_SRC, width: SH_W, height: SH_H}));
    return n;
  };
  // ⚠️ Тёмной вуали НЕТ: фон под цитату не гасится вообще (правка автора).

  // ═══ Страницы Record Handling ════════════════════════════════════════
  const T = (TILT * Math.PI) / 180;
  const paperToAsset = (px: number, py: number) => {
    const dx = px - PAPER_W / 2;
    const dy = py - PAPER_H / 2;
    return [
      RH_W / 2 + dx * Math.cos(T) - dy * Math.sin(T),
      RH_H / 2 + dx * Math.sin(T) + dy * Math.cos(T),
    ] as const;
  };
  const toRig = (ax: number, ay: number) => [ax - RH_W / 2, ay - RH_H / 2] as const;
  const rigAt = (p: {x: number; y: number}, s: number) => {
    const [ax, ay] = paperToAsset(p.x, p.y);
    const [lx, ly] = toRig(ax, ay);
    return new Vector2(-lx * s, -ly * s);
  };
  const screenOf = (px: number, py: number, at: {x: number; y: number}, s: number) => {
    const [ax, ay] = paperToAsset(px, py);
    const [lx, ly] = toRig(ax, ay);
    const rig = rigAt(at, s);
    return [rig.x + lx * s, rig.y + ly * s] as const;
  };
  // ⚠️ Все штрихи одной страницы живут в ОДНОМ кэшированном слое, а multiply
  // и прозрачность стоят на СЛОЕ, не на полосках. Иначе соседние полоски
  // (строки объявления стоят гуще своей высоты, перенос null тоже) множатся
  // дважды и пересечения темнее самих полосок. Кэш сплющивает их до ровной
  // краски до композита с бумагой.
  const makeHlLayer = (host: Node) => {
    const layer = new Node({compositeOperation: 'multiply', opacity: 0.82});
    layer.cache(true);
    layer.cachePadding(8);
    host.add(layer);
    return layer;
  };
  const makeHighlight = (layer: Node, hl: {x0: number; x1: number; cy: number; h: number}) => {
    const [ax, ay] = paperToAsset(hl.x0, hl.cy);
    const [lx, ly] = toRig(ax, ay);
    const pad = hl.h * 0.2;
    const r = new Rect({
      x: lx - pad * Math.cos(T),
      y: ly - pad * Math.sin(T),
      offset: [-1, 0],
      rotation: TILT,
      width: 0,
      height: hl.h,
      fill: HL_FILL,
    });
    layer.add(r);
    return {rect: r, w: hl.x1 - hl.x0 + pad * 2};
  };

  const rh9Blur = createSignal(0);
  const rh9 = createRef<Node>();
  stage().add(
    <Node ref={rh9} scale={RH_REVEAL_S} position={rigAt(RH_AT_REVEAL, RH_REVEAL_S)} opacity={0} filters={[blur(() => rh9Blur())]} />,
  );
  rh9().add(new Img({src: RH9_SRC, width: RH_W, height: RH_H}));
  const rh9Hl = makeHlLayer(rh9());
  const hlDecl = [HL_DECL_1, HL_DECL_2, HL_DECL_3].map(h => makeHighlight(rh9Hl, h));
  const hlPartial = makeHighlight(rh9Hl, HL_PARTIAL);
  const hlNullA = makeHighlight(rh9Hl, HL_NULL_A);
  const hlNullB = makeHighlight(rh9Hl, HL_NULL_B);
  // финал живёт на стр. 9: признание читается поверх её приглушённого скана
  const rh9Scrim = new Rect({width: RH_W, height: RH_H, fill: 'rgb(8, 9, 12)', opacity: 0});
  rh9().add(rh9Scrim);

  const rh11Blur = createSignal(0);
  const rh11 = createRef<Node>();
  // ⚠️ Чертёж рождается сразу на ПУШ-кадровке: запись 1908 в центре экрана,
  // прямо под финальное положение морфящейся карточки.
  stage().add(
    <Node
      ref={rh11}
      scale={DIAG_S_BOX * GRAPH_S}
      position={rigAt(DIAG_AT_BOX, DIAG_S_BOX * GRAPH_S)}
      opacity={0}
      filters={[blur(() => rh11Blur())]}
    />,
  );
  rh11().add(new Img({src: RH11_SRC, width: RH_W, height: RH_H}));
  const rh11Hl = makeHlLayer(rh11());
  const boxNulls = NULLS_1908.map(n => makeHighlight(rh11Hl, n));
  const restNulls = NULLS_REST.map(n => makeHighlight(rh11Hl, n));

  // ═══ Лист ALGOL: экспонат на плоскости фильма ════════════════════════
  // Стоит за правым краем и заезжает в кадр; лампы на нём нет — она была
  // выделением под зум, а зума больше нет.
  const algolPage = new Node({
    x: PAGE_X + PAGE_DX,
    y: PAGE_Y,
    scale: PAGE_SCALE,
    rotation: PAGE_TILT_IN,
    opacity: 0,
    // ⚠️ Белый листа приглушён на 7% (правка автора): скан идёт 255/255/255,
    // а самое светлое место архивного снимка — 169, поэтому чистая бумага
    // читалась наклеенной. 0.93 сажает её в тон отпечатка, не превращая в
    // «упавшую прозрачность»: сдвиг ровно на границе заметности.
    filters: [brightness(PAGE_LIFT)],
  });
  // ⚠️ ЩЕЛЬ СВЕТА живёт ВНУТРИ листа, в окне по размеру бумаги с clip: свет
  // выделяет заголовок, но не трогает ни стену, ни Хоара.
  // ⚠️ Градиент ЛИНЕЙНЫЙ по вертикали, а не радиальный: полоса обязана идти во
  // всю ширину листа и зависеть только от y. Радиус этого дать не может.
  const spotWin = new Rect({width: SH_W, height: SH_H, clip: true, opacity: 0});
  const fy = (y: number) => (y + SH_H / 2) / SH_H;          // локальная y → доля 0…1
  const D = 'rgba(20, 20, 24, ';
  spotWin.add(
    new Rect({
      width: SH_W,
      height: SH_H,
      fill: new Gradient({
        type: 'linear',
        from: new Vector2(0, -SH_H / 2),
        to: new Vector2(0, SH_H / 2),
        stops: [
          {offset: 0, color: `${D}${BAND_DARK})`},
          {offset: fy(BAND_Y - BAND_HALF - BAND_FEATHER), color: `${D}${BAND_DARK})`},
          {offset: fy(BAND_Y - BAND_HALF), color: `${D}0)`},
          {offset: fy(BAND_Y + BAND_HALF), color: `${D}0)`},
          {offset: fy(BAND_Y + BAND_HALF + BAND_FEATHER), color: `${D}${BAND_DARK})`},
          {offset: 1, color: `${D}${BAND_DARK})`},
        ],
      }),
    }),
  );
  // ⚠️ Второй слой — КОНЦЫ ЛУЧА: спад к левой и правой кромке листа. Без него
  // щель идёт от края до края с постоянным коэффициентом и читается плашкой.
  const fx = (x: number) => (x + SH_W / 2) / SH_W;
  spotWin.add(
    new Rect({
      width: SH_W,
      height: SH_H,
      fill: new Gradient({
        type: 'linear',
        from: new Vector2(-SH_W / 2, 0),
        to: new Vector2(SH_W / 2, 0),
        stops: [
          {offset: 0, color: `${D}${BAND_EDGE})`},
          {offset: fx(-BAND_EDGE_X), color: `${D}0)`},
          {offset: fx(BAND_EDGE_X), color: `${D}0)`},
          {offset: 1, color: `${D}${BAND_EDGE})`},
        ],
      }),
    }),
  );
  // ⚠️ Порядок сборки листа: сначала УСИЛЕННАЯ ТЕНЬ (её тело ровно под бумагой
  // и потому не видно — видно только тень), потом сам лист, потом щель света.
  // Прозрачность тени привязана к прозрачности щели: свет гаснет — тень
  // растёт, и бумага никогда не оказывается темнее того, что сама отбрасывает.
  const shadowBoost = new Rect({
    width: SH_W,
    height: SH_H,
    fill: 'rgb(232, 228, 219)',
    shadowColor: 'rgba(0,0,0,0.42)',
    shadowBlur: 210,
    shadowOffset: new Vector2(10, 18),
    opacity: () => spotWin.opacity(),
  });
  algolPage.add(shadowBoost);
  algolPage.add(makeAlgolSheet());
  algolPage.add(spotWin);
  stage().add(algolPage);

  // ═══ Титул Record Handling: третий житель того же слота ══════════════
  // ⚠️ ДВА узла одного и того же ассета, снятые в одной посадке:
  //   rhTitle    — лист, ОБРЕЗАННЫЙ по кромке бумаги: предмет, который можно
  //                привезти в комнату и потом снять со стопки;
  //   rhTitleBed — тот же кадр ЦЕЛИКОМ, вместе со столом: он проступает на
  //                подходе, и вокруг листа появляется его собственный стол.
  // Оба идут одним масштабом и позицией, поэтому бумага совпадает пиксель в
  // пиксель, и переход виден только по краям — как приход контекста.
  const titleScale = TITLE_W / PAPER_W;
  const rhSheet = (src: string) => {
    const n = new Node({});
    n.add(
      new Rect({
        width: PAPER_W,
        height: PAPER_H,
        rotation: TILT,
        fill: 'rgb(234, 226, 209)',
        shadowColor: 'rgba(0,0,0,0.42)',
        shadowBlur: 64,
        shadowOffset: new Vector2(10, 18),
      }),
    );
    const win = new Rect({width: PAPER_W, height: PAPER_H, rotation: TILT, clip: true});
    const inner = new Node({rotation: -TILT});
    inner.add(new Img({src, width: RH_W, height: RH_H}));
    win.add(inner);
    n.add(win);
    return n;
  };
  const rhTitleBed = createRef<Node>();
  stage().add(<Node ref={rhTitleBed} scale={titleScale} x={PAGE_X} opacity={0} rotation={TITLE_TILT} />);
  rhTitleBed().add(new Img({src: RH1_SRC, width: RH_W, height: RH_H}));

  const rhTitle = createRef<Node>();
  stage().add(
    <Node ref={rhTitle} scale={titleScale} x={PAGE_X + TITLE_DX} rotation={TITLE_TILT_IN} opacity={0} />,
  );
  rhTitle().add(rhSheet(RH1_SRC));

  // ═══ ЗЕРНО НА ВЕСЬ КАДР ══════════════════════════════════════════════
  // ⚠️ Главная претензия автора: «страница лежит поверх фотографии, а должна
  // ощущаться частью единого архивного разворота». Общее зерно — то, что
  // физически объединяет вклейку с отпечатком: серый 128 + шум поверх всего
  // в режиме overlay (128 = без изменений, отклонения светлят/темнят).
  // ⚠️ Гаснет на подходе к документу: у сканов Record Handling зерно своё,
  // запечённое в ассет, второе поверх него дало бы кашу.
  const grain = new Img({
    src: GRAIN_SRC,
    width: Screen.width,
    height: Screen.height,
    compositeOperation: 'overlay',
    opacity: GRAIN_OP,
  });
  stage().add(grain);

  // 0 пока бумага — предмет в комнате, 1 когда вокруг неё уже её стол
  const bigPage = createSignal(0);

  // ⚠️ Виньетка нужна бумаге НА ТЁМНОМ ФОНЕ: без неё скан читается плоским
  // белым прямоугольником. На тёмных кадрах (1965, графит) её центр —
  // прозрачный круг посреди затемнённых краёв, и это видно как светлый диск,
  // который разваливает кадр. Поэтому прозрачность привязана к бумаге.
  // ⚠️ Лист ALGOL входит сюда ЧЕРЕЗ СИГНАЛ, а не через свою прозрачность: пока
  // он висит поверх снимка, виньетка приглушила бы углы самой фотографии; она
  // нужна ему только когда он сам стал кадром (см. bigPage в GROW).
  stage().add(
    new Rect({
      opacity: () => Math.max(rh9().opacity(), rh11().opacity(), bigPage()),
      width: Screen.width * 1.2,
      height: Screen.height * 1.2,
      fill: new Gradient({
        type: 'radial',
        from: new Vector2(0, -60),
        to: new Vector2(0, -60),
        fromRadius: 380,
        toRadius: 1320,
        stops: [
          {offset: 0, color: 'rgba(10,11,14,0)'},
          {offset: 0.55, color: 'rgba(10,11,14,0.16)'},
          {offset: 1, color: 'rgba(10,11,14,0.62)'},
        ],
      }),
    }),
  );

  // ═══════════════ ГРАФИТ ══════════════════════════════════════════════
  // Один узел на всю графитовую часть: он же отъезжает камерой, он же
  // приглушается, когда сверху приходит бумага.
  const graph = createRef<Node>();
  stage().add(<Node ref={graph} opacity={0} />);
  // ⚠️ Рабочее пространство (код, карточки, стрелка, строка создания) — своим
  // узлом: перед приходом бумаги графит должен ОПУСТЕТЬ до одной фразы, иначе
  // на кроссфейде код и машинопись накладываются и кадр превращается в кашу.
  const work = createRef<Node>();
  graph().add(<Node ref={work} />);

  // ── объявление: каждый токен знает своё место НА ЛИСТЕ и место В СТОЛБЦЕ
  type Tok = {t: string; c: string; pl: readonly [number, number]; cl: readonly [number, number]};
  const pageAt = (lineX: number, lineY: number, n: number) => {
    const [sx, sy] = screenOf(lineX, lineY, AT_DECL, RH_READ_S);
    return [sx + n * CODE_ADV, sy] as const;
  };
  const L1 = (n: number) => pageAt(120, 1010, n);
  const L2 = (n: number) => pageAt(500, 1050, n);
  const L3 = (n: number) => pageAt(1245, 1089, n);
  const L4 = (n: number) => pageAt(120, 1132, n);

  const DECL: Tok[] = [
    {t: 'record class ', c: KW, pl: L1(0), cl: colAt(0, 0)},
    {t: 'person ', c: TY, pl: L1(13), cl: colAt(0, 13)},
    {t: '(', c: PUN, pl: L1(20), cl: colAt(0, 20)},
    {t: 'integer ', c: TY, pl: L1(21), cl: colAt(1, 2)},
    {t: 'date of birth', c: IDENT, pl: L1(29), cl: colAt(1, 10)},
    {t: ';', c: PUN, pl: L1(42), cl: colAt(1, 23)},
    {t: 'Boolean ', c: TY, pl: L1(44), cl: colAt(2, 2)},
    {t: 'male', c: IDENT, pl: L1(52), cl: colAt(2, 10)},
    {t: ';', c: PUN, pl: L1(56), cl: colAt(2, 14)},
    {t: 'reference ', c: TY, pl: L2(0), cl: colAt(3, 2)},
    {t: '(', c: PUN, pl: L2(10), cl: colAt(3, 12)},
    {t: 'person', c: TY, pl: L2(11), cl: colAt(3, 13)},
    {t: ') ', c: PUN, pl: L2(17), cl: colAt(3, 19)},
    {t: 'father', c: IDENT, pl: L2(19), cl: colAt(3, 21)},
    {t: ',', c: PUN, pl: L2(25), cl: colAt(3, 27)},
    {t: 'elder sibling', c: IDENT, pl: L2(27), cl: colAt(4, 21)},
    {t: ',', c: PUN, pl: L2(40), cl: colAt(4, 34)},
    {t: 'youngest', c: IDENT, pl: L2(42), cl: colAt(5, 21)},
    {t: 'offspring', c: IDENT, pl: L3(0), cl: colAt(5, 30)},
    {t: ');', c: PUN, pl: L3(9), cl: colAt(6, 0)},
    // строка переменных с его же страницы — даёт имена T, J, K строкам создания
    {t: 'reference ', c: TY, pl: L4(0), cl: colAt(7, 0)},
    {t: '(', c: PUN, pl: L4(10), cl: colAt(7, 10)},
    {t: 'person', c: TY, pl: L4(11), cl: colAt(7, 11)},
    {t: ') ', c: PUN, pl: L4(17), cl: colAt(7, 17)},
    {t: 'T', c: IDENT, pl: L4(19), cl: colAt(7, 19)},
    {t: ', ', c: PUN, pl: L4(20), cl: colAt(7, 20)},
    {t: 'J', c: IDENT, pl: L4(22), cl: colAt(7, 22)},
    {t: ', ', c: PUN, pl: L4(23), cl: colAt(7, 23)},
    {t: 'K', c: IDENT, pl: L4(25), cl: colAt(7, 25)},
    {t: ';', c: PUN, pl: L4(26), cl: colAt(7, 26)},
  ];
  const mkTok = (t: string, c: string, p: readonly [number, number], op = 1) =>
    new Txt({
      text: t,
      x: p[0],
      y: p[1],
      offset: [-1, 0],
      fontFamily: MONO,
      fontSize: CODE_FS,
      fontWeight: 500,
      fill: c,
      opacity: op,
    });
  const declNodes = DECL.map(d => {
    const n = mkTok(d.t, d.c, d.pl);
    work().add(n);
    return n;
  });
  // ⚠️ `reference (person)` в столбце повторяется трижды — две копии рождаются
  // на месте оригинала и проявляются по дороге к своим строкам.
  const PREFIX: {t: string; c: string; n: number}[] = [
    {t: 'reference ', c: TY, n: 0},
    {t: '(', c: PUN, n: 10},
    {t: 'person', c: TY, n: 11},
    {t: ') ', c: PUN, n: 17},
  ];
  const dupNodes: {node: Txt; to: readonly [number, number]}[] = [];
  for (const row of [4, 5]) {
    for (const p of PREFIX) {
      const n = mkTok(p.t, p.c, L2(p.n), 0);
      work().add(n);
      dupNodes.push({node: n, to: colAt(row, 2 + p.n)});
    }
  }
  // строки столбца — чтобы гасить всё, кроме читаемого поля
  const rowNodes: Txt[][] = [[], [], [], [], [], [], [], []];
  declNodes.forEach((n, i) => rowNodes[Math.round((DECL[i].cl[1] - COL_Y0) / ROW_H)].push(n));
  dupNodes.forEach(d => rowNodes[Math.round((d.to[1] - COL_Y0) / ROW_H)].push(d.node));

  // ── карточка: пять отдельных клеток, каждая проявляется на своём поле ─
  const card = createRef<Node>();
  work().add(<Node ref={card} />);
  const cells: Rect[] = [];
  for (let i = 0; i < 5; i++) {
    const r = new Rect({
      x: CARD_X,
      y: cellY(i),
      width: CARD_W,
      height: ROW_H,
      stroke: CARD_STROKE,
      lineWidth: 2,
      opacity: 0,
    });
    card().add(r);
    cells.push(r);
  }
  const cardLabel = new Txt({
    text: 'person',
    x: CARD_X,
    y: cellY(0) - ROW_H / 2 - 44,
    fontFamily: MONO,
    fontSize: CARD_LABEL_FS,
    fontWeight: 500,
    fill: TY,
    opacity: 0,
  });
  card().add(cardLabel);

  const cellVal = (i: number, text: string, fill: string) => {
    const n = new Txt({
      text,
      x: CARD_X,
      y: cellY(i),
      fontFamily: MONO,
      fontSize: CARD_VAL_FS,
      fontWeight: 500,
      fill,
      opacity: 0,
    });
    card().add(n);
    return n;
  };
  const val1908 = cellVal(0, '1908', KW);
  const valTrue = cellVal(1, 'true', KW);
  const cellNulls = [cellVal(2, 'null', KW), cellVal(3, 'null', KW)];

  // ── вторая карточка: младший потомок, куда уходит стрелка ────────────
  const other = createRef<Node>();
  work().add(<Node ref={other} opacity={0} />);
  other().add(
    new Rect({
      x: OTHER_X,
      y: OTHER_Y,
      width: CARD_W,
      height: CARD_H,
      stroke: CARD_STROKE,
      lineWidth: 2,
      radius: 4,
    }),
  );
  for (let i = 1; i < 5; i++) {
    other().add(
      new Line({
        points: [
          [OTHER_X - CARD_W / 2, OTHER_Y - CARD_H / 2 + ROW_H * i],
          [OTHER_X + CARD_W / 2, OTHER_Y - CARD_H / 2 + ROW_H * i],
        ],
        stroke: CARD_STROKE,
        lineWidth: 2,
      }),
    );
  }
  // ⚠️ Имя K — его собственное из статьи (`reference (person) T, J, K;`).
  // Ярлык у второй карточки — имя, а не класс: класс уже показан у первой.
  other().add(
    new Txt({
      text: 'K',
      x: OTHER_X,
      y: OTHER_Y - CARD_H / 2 - 44,
      fontFamily: MONO,
      fontSize: CARD_LABEL_FS,
      fontWeight: 500,
      fill: IDENT,
    }),
  );
  // значения K заполняет ЕГО СОБСТВЕННАЯ строка создания (вторая)
  const mkCardVal = (parent: Node, cx: number, topY: number, i: number, text: string) => {
    const n = new Txt({
      text,
      x: cx,
      y: topY + ROW_H * (i + 0.5),
      fontFamily: MONO,
      fontSize: CARD_VAL_FS,
      fontWeight: 500,
      fill: KW,
      opacity: 0,
    });
    parent.add(n);
    return n;
  };
  const kTop = OTHER_Y - CARD_H / 2;
  const kVals = ['1935', 'true', 'null', 'null'].map((t, i) => mkCardVal(other(), OTHER_X, kTop, i, t));

  // ⚠️ Стрелка — ЧИСТАЯ ГОРИЗОНТАЛЬ из пятой клетки в первую клетку K
  // (высоты совпадают по построению OTHER_Y). Цель видна в кадре ЦЕЛИКОМ.
  const arrow = new Line({
    points: [
      [CARD_X + CARD_W / 2, cellY(4)],
      [OTHER_X - CARD_W / 2, cellY(4)],
    ],
    stroke: 'rgba(244, 241, 235, 0.55)',
    lineWidth: 3,
    endArrow: true,
    arrowSize: 12,
    end: 0,
  });
  work().add(arrow);

  // ── третья запись: 1964, младший потомок K ───────────────────────────
  const card3 = createRef<Node>();
  work().add(<Node ref={card3} opacity={0} />);
  card3().add(
    new Rect({
      x: C3_X,
      y: C3_Y,
      width: CARD_W,
      height: CARD_H,
      stroke: CARD_STROKE,
      lineWidth: 2,
      radius: 4,
    }),
  );
  for (let i = 1; i < 5; i++) {
    card3().add(
      new Line({
        points: [
          [C3_X - CARD_W / 2, C3_Y - CARD_H / 2 + ROW_H * i],
          [C3_X + CARD_W / 2, C3_Y - CARD_H / 2 + ROW_H * i],
        ],
        stroke: CARD_STROKE,
        lineWidth: 2,
      }),
    );
  }
  card3().add(
    new Txt({
      text: 'J',
      x: C3_X,
      y: C3_Y - CARD_H / 2 - 44,
      fontFamily: MONO,
      fontSize: CARD_LABEL_FS,
      fontWeight: 500,
      fill: IDENT,
    }),
  );
  // значения J заполняет ТРЕТЬЯ строка — и кончается тремя null подряд
  const jTop = C3_Y - CARD_H / 2;
  const jVals = ['1964', 'true', 'null', 'null', 'null'].map((t, i) => mkCardVal(card3(), C3_X, jTop, i, t));
  // стрелка K → 1964: из пятой клетки K влево, в первую клетку 1964
  const arrow3 = new Line({
    points: [
      [OTHER_X - CARD_W / 2, K_CELL5_Y],
      [C3_X + CARD_W / 2, K_CELL5_Y],
    ],
    stroke: 'rgba(244, 241, 235, 0.55)',
    lineWidth: 3,
    endArrow: true,
    arrowSize: 12,
    end: 0,
  });
  work().add(arrow3);
  const dot3 = new Rect({
    x: OTHER_X,
    y: K_CELL5_Y,
    width: 12,
    height: 12,
    radius: 6,
    fill: 'rgba(244, 241, 235, 0.8)',
    opacity: 0,
  });
  work().add(dot3);
  const arrowDot = new Rect({
    x: CARD_X,
    y: cellY(4),
    width: 12,
    height: 12,
    radius: 6,
    fill: 'rgba(244, 241, 235, 0.8)',
    opacity: 0,
  });
  work().add(arrowDot);

  // ── ТРИ строки создания ─────────────────────────────────────────────
  // ⚠️ Печать с ОБРЫВАМИ в первой строке (father — долгий, здесь сидел
  // Хоар; sibling — короткий) и КАСКАДОМ дальше: аргумент K закрывает
  // строку → появляется запись K → её заполняет ВТОРАЯ строка; аргумент J
  // закрывает вторую → семья отъезжает, появляется J → ТРЕТЬЯ строка
  // кончается тремя null подряд. Дыр всё больше.
  type MTok = {
    t: string;
    c: string;
    line: number;
    cell?: Txt;
    dot?: Rect;
    act?: 'cardK' | 'cardJ';
    stall?: number;
  };
  const MAKE: MTok[] = [
    {t: 'T ', c: IDENT, line: 0},
    {t: ':= ', c: PUN, line: 0},
    {t: 'person', c: TY, line: 0},
    {t: '(', c: PUN, line: 0},
    {t: '1908', c: KW, line: 0, cell: val1908},
    {t: ', ', c: PUN, line: 0},
    {t: 'true', c: KW, line: 0, cell: valTrue},
    {t: ', ', c: PUN, line: 0},
    {t: 'null', c: KW, line: 0, cell: cellNulls[0], stall: FATHER_STALL},
    {t: ', ', c: PUN, line: 0},
    {t: 'null', c: KW, line: 0, cell: cellNulls[1], stall: SIB_STALL},
    {t: ', ', c: PUN, line: 0},
    {t: 'K', c: IDENT, line: 0, dot: arrowDot},
    {t: ');', c: PUN, line: 0, act: 'cardK'},
    {t: 'K ', c: IDENT, line: 1},
    {t: ':= ', c: PUN, line: 1},
    {t: 'person', c: TY, line: 1},
    {t: '(', c: PUN, line: 1},
    {t: '1935', c: KW, line: 1, cell: kVals[0]},
    {t: ', ', c: PUN, line: 1},
    {t: 'true', c: KW, line: 1, cell: kVals[1]},
    {t: ', ', c: PUN, line: 1},
    {t: 'null', c: KW, line: 1, cell: kVals[2]},
    {t: ', ', c: PUN, line: 1},
    {t: 'null', c: KW, line: 1, cell: kVals[3]},
    {t: ', ', c: PUN, line: 1},
    {t: 'J', c: IDENT, line: 1, dot: dot3},
    {t: ');', c: PUN, line: 1, act: 'cardJ'},
    {t: 'J ', c: IDENT, line: 2},
    {t: ':= ', c: PUN, line: 2},
    {t: 'person', c: TY, line: 2},
    {t: '(', c: PUN, line: 2},
    {t: '1964', c: KW, line: 2, cell: jVals[0]},
    {t: ', ', c: PUN, line: 2},
    {t: 'true', c: KW, line: 2, cell: jVals[1]},
    {t: ', ', c: PUN, line: 2},
    {t: 'null', c: KW, line: 2, cell: jVals[2]},
    {t: ', ', c: PUN, line: 2},
    {t: 'null', c: KW, line: 2, cell: jVals[3]},
    {t: ', ', c: PUN, line: 2},
    {t: 'null', c: KW, line: 2, cell: jVals[4]},
    {t: ');', c: PUN, line: 2},
  ];
  const makeRow = createRef<Node>();
  work().add(<Node ref={makeRow} x={COL_X + COL_DX} y={MAKE_Y + BLOCK_GAP * 2} opacity={0} />);
  const lineChars = [0, 0, 0];
  const makeNodes = MAKE.map(t => {
    const n = mkTok('', t.c, [lineChars[t.line] * CODE_ADV, t.line * MAKE_PITCH]);
    lineChars[t.line] += t.t.length;
    makeRow().add(n);
    return n;
  });
  const makeCaret = new Rect({
    offset: [-1, 0],
    width: CODE_ADV * 0.72,
    height: CODE_FS * 1.05,
    fill: 'rgba(232, 207, 174, 0.9)',
    opacity: 0,
  });
  makeRow().add(makeCaret);

  // ═══ Его слова — нежным роузом ═══════════════════════════════════════
  const easyBlur = createSignal(10);
  const easy = createRef<Node>();
  stage().add(<Node ref={easy} opacity={0} filters={[blur(() => easyBlur())]} />);
  easy().add(
    <Txt
      text={'“…simply because it was so easy to implement.”'}
      fontFamily={MONO}
      fontSize={50}
      fontWeight={500}
      fill={ROSE}
    />,
  );

  // ═══ Лента языков (живёт внутри финальной полосы) ════════════════════
  const belt = createRef<Node>();
  stage().add(<Node ref={belt} opacity={0} />);
  const pairOps: SimpleSignal<number>[] = [];
  const pairCenters: number[] = [];
  {
    // кумулятивная раскладка: ширина пары = имя + зазор + конструкция,
    // между парами равный зазор
    let cursorX = 0;
    LANGS.forEach((lang, i) => {
      const nameW = lang.name.length * BELT_CHAR;
      const formW = lang.form.length * BELT_CHAR;
      const pairW = nameW + BELT_INNER + formW;
      const pairX = cursorX;
      cursorX += pairW + BELT_GAP;
      pairCenters.push(pairX + pairW / 2);

      const op = createSignal(i === 0 ? 1 : BELT_DIM);
      pairOps.push(op);
      const pair = new Node({x: pairX, opacity: () => op()});
      pair.add(
        new Txt({
          text: lang.name,
          offset: [-1, 0],
          fontFamily: MONO,
          fontSize: BELT_FS,
          fontWeight: 500,
          fill: BELT_NAME,
        }),
      );
      pair.add(
        new Txt({
          text: lang.form,
          x: nameW + BELT_INNER,
          offset: [-1, 0],
          fontFamily: MONO,
          fontSize: BELT_FS,
          fontWeight: 500,
          fill: BELT_FORM,
        }),
      );
      belt().add(pair);
    });
  }

  // ═══ Шторки финала ═══════════════════════════════════════════════════
  const makeBackdrop = () => {
    const n = new Node({});
    n.add(
      new Rect({
        width: Screen.width,
        height: Screen.height,
        fill: new Gradient({
          type: 'linear',
          from: new Vector2(0, -Screen.height / 2),
          to: new Vector2(0, Screen.height / 2),
          stops: [
            {offset: 0, color: Colors.background.from},
            {offset: 1, color: Colors.background.to},
          ],
        }),
      }),
    );
    n.add(
      new Rect({
        width: Screen.width,
        height: Screen.height,
        fill: new Gradient({
          type: 'radial',
          from: new Vector2(Screen.width * 0.12, -Screen.height * 0.12),
          to: new Vector2(Screen.width * 0.12, -Screen.height * 0.12),
          fromRadius: 0,
          toRadius: Screen.width * 0.95,
          stops: [
            {offset: 0, color: 'rgba(246,231,212,0.045)'},
            {offset: 1, color: 'rgba(255,255,255,0)'},
          ],
        }),
      }),
    );
    return n;
  };
  const makeShutter = (sign: -1 | 1) => {
    const shut = new Rect({width: Screen.width, height: SHUT_H, y: sign * (Screen.height / 2 + SHUT_H / 2 + 20), clip: true});
    const backdrop = makeBackdrop();
    backdrop.position.y(() => -shut.position.y());
    shut.add(backdrop);
    view.add(shut);
    return shut;
  };
  const shutterTop = makeShutter(-1);
  const shutterBot = makeShutter(1);

  // ═══════════════ ТАЙМЛАЙН ════════════════════════════════════════════
  yield* waitFor(0.5);

  cursor.opacity(1);
  for (let i = 1; i <= YEAR.length; i++) {
    yield* waitFor(YEAR_KEY);
    yearTxt.text(YEAR.slice(0, i));
    cursor.position.x(yearLeft + YEAR_ADV * i + YEAR_ADV * 0.18);
  }
  // ⚠️ Курсор МИГАЕТ РОВНО до самого прихода света (правка автора: «курсор
  // останавливается перед переходом, перестаёт мигать, это странно»). Было
  // одно моргание и 0.3с неподвижного свечения — рифма обрывалась раньше
  // кадра. Теперь ровный такт 2.3 Гц, четыре полутакта, последний — светящийся:
  // свет приходит на горящем курсоре и выжигает его вместе с годом.
  for (let i = 0; i < CARET_STEPS; i++) {
    cursor.opacity(i % 2 === 0 ? 0 : 1);
    yield* waitFor(CARET_HALF);
  }
  cursor.opacity(1);

  // ⚠️ ПРОЯВКА. Наезд стартует ВМЕСТЕ со светом и держится весь настенный
  // блок — снимок рождается уже в движении и не замирает. Вырезка Хоара
  // едет своим, большим наездом: он выходит из стены, а не растёт с ней.
  yield all(
    photo().scale(PHOTO_S * PUSH_K, PUSH_D, easeInOutSine),
    photo().position(new Vector2(PUSH_X1, PUSH_Y1), PUSH_D, easeInOutSine),
    hoareLayer().scale(HOARE_K, PUSH_D, easeInOutSine),
    hoareLayer().position(
      new Vector2(toLayer(FIG_CX) * (1 - HOARE_K) + HOARE_DX, HOARE_BASE_Y * (1 - HOARE_K)),
      PUSH_D,
      easeInOutSine,
    ),
  );
  // свет приходит → год белеет и выгорает в нём → отпечаток набирает
  // плотность → и только потом из него медленно уходит тепло
  yield* all(
    photo().opacity(1, DEV_LIGHT, easeOutSine),
    yearTxt.fill(YEAR_BURN, DEV_LIGHT * 0.5, easeOutSine),
    cursor.fill(YEAR_BURN, DEV_LIGHT * 0.5, easeOutSine),
    chain(
      waitFor(DEV_LIGHT * 0.34),
      all(
        yearTxt.opacity(0, DEV_LIGHT * 0.66, easeInCubic),
        cursor.opacity(0, DEV_LIGHT * 0.5, easeInCubic),
      ),
    ),
    devSoft(0, DEV_SHARP, easeOutCubic),
    devWarm(0, DEV_GRADE, easeInOutSine),
  );
  yearWrap.remove();
  photo().filters([]);
  yield* waitFor(SETTLE);

  // ═══ 2а. Экспонат: лист ALGOL заезжает справа ═══════════════════════
  // ⚠️ Приходит КАК ОДНО: ход, наклон и проявление — один жест, ничего не
  // появляется по частям. Прозрачность снимается в первой трети хода, чтобы
  // лист не «включался» на месте и не летел через кадр видимым.
  yield* all(
    algolPage.position.x(PAGE_X, PAGE_IN, easeOutCubic),
    algolPage.rotation(PAGE_TILT, PAGE_IN, easeOutCubic),
    algolPage.opacity(1, PAGE_IN * 0.42, easeOutSine),
  );
  // ⚠️ Свет ложится на заголовок статьи и снимается до «Forty-four years
  // later»: к приговору бумага снова ровная. Сумма фаз = PAGE_HOLD.
  yield* spotWin.opacity(1, SPOT_IN, easeInOutCubic);
  yield* waitFor(SPOT_HOLD);
  yield* spotWin.opacity(0, SPOT_OUT, easeInOutCubic);
  yield* waitFor(PAGE_HOLD - SPOT_IN - SPOT_HOLD - SPOT_OUT);

  // ═══ 2б. «…his billion-dollar mistake» — лист уезжает за кадр и ЛЕВОЙ
  // КРОМКОЙ ОТКРЫВАЕТ приговор. Цитата написана на стене заранее (её просто
  // не было видно под бумагой), поэтому после свапа она уже там — ни фейда,
  // ни отдельного появления. Уход без гашения: полупрозрачный лист над
  // надписью дал бы двойную экспозицию.
  qLine1.opacity(1);
  qLine2.opacity(1);
  attrib().opacity(1);
  yield* all(
    algolPage.position.x(PAGE_X + PAGE_OUT_DX, PAGE_OUT, easeInCubic),
    algolPage.rotation(PAGE_TILT_IN, PAGE_OUT, easeInCubic),
  );
  algolPage.remove();
  yield* waitFor(QUOTE_HOLD);

  // ═══ 3. «The mistake itself — far less so» — ОДИН ХОД: жёлтый титул входит
  // справа, накрывает собой приговор (улика встаёт на место фразы) и не
  // останавливается — идёт к нам до читаемой крупности. Комната уступает под
  // ним, вокруг листа проступает его собственный стол (rhTitleBed — тот же
  // кадр целиком). Контекст приходит вместе с документом, а не отдельно.
  // ⚠️ Порядок внутри хода: сначала бумага (0…25%) — иначе цитата мигнёт
  // из-под неё; потом, с 45%, стол; комната гаснет с 50%, когда лист уже
  // накрывает надпись целиком (замер: на половине хода лист −52…972 против
  // цитаты 314…886).
  const titleAt = rigAt(RH_AT_REVEAL, RH_REVEAL_S);
  rhTitleBed().position(rhTitle().position());
  rhTitleBed().rotation(TITLE_TILT_IN);
  yield* all(
    rhTitle().opacity(1, TITLE_COME * 0.18, easeOutSine),
    rhTitle().position(titleAt, TITLE_COME, easeOutCubic),
    rhTitle().scale(RH_REVEAL_S, TITLE_COME, easeOutCubic),
    rhTitle().rotation(0, TITLE_COME, easeOutCubic),
    rhTitleBed().position(titleAt, TITLE_COME, easeOutCubic),
    rhTitleBed().scale(RH_REVEAL_S, TITLE_COME, easeOutCubic),
    rhTitleBed().rotation(0, TITLE_COME, easeOutCubic),
    // ⚠️ easeOutCubic съедает 72% пути за первые 35% времени, поэтому стол и
    // уход комнаты можно ставить раньше: на 0.35 лист уже −343…829 и цитату
    // (194…766) накрывает целиком. Слайд доигрывается уже над столом.
    chain(waitFor(TITLE_COME * 0.3), rhTitleBed().opacity(1, TITLE_COME * 0.45, easeInOutCubic)),
    chain(waitFor(TITLE_COME * 0.35), room().opacity(0, TITLE_COME * 0.45, easeInOutCubic)),
    chain(waitFor(TITLE_COME * 0.3), bigPage(1, TITLE_COME * 0.45, easeInOutCubic)),
    chain(waitFor(TITLE_COME * 0.3), grain.opacity(0, TITLE_COME * 0.45, easeInOutCubic)),
  );
  room().remove();
  grain.remove();
  yield* waitFor(TITLE_READ);

  // ═══ 5. Титул СНИМАЮТ — под ним страница 9. Обрезанный по кромке лист
  // уходит вверх-влево за кадр, а стол под ним уже принадлежит стр. 9:
  // rhTitleBed уступает ей место коротким кроссфейдом под движущимся листом,
  // поэтому зерно столешницы не ловится глазом.
  rh9().opacity(0);
  yield* all(
    rhTitle().position(titleAt.add(new Vector2(-LIFT_OUT, -520)), LIFT, easeInCubic),
    rhTitle().rotation(-5.5, LIFT, easeInCubic),
    rh9().opacity(1, LIFT * 0.3, easeInOutSine),
    rhTitleBed().opacity(0, LIFT * 0.3, easeInOutSine),
  );
  rhTitle().remove();
  rhTitleBed().remove();
  yield* waitFor(LIFT_SETTLE);

  // ═══ 6. Улика: маркер по ВСЕМ ТРЁМ строкам объявления ═══════════════
  yield* all(
    rh9().scale(RH_READ_S, RH_PUSH, easeInOutCubic),
    rh9().position(rigAt(AT_DECL, RH_READ_S), RH_PUSH, easeInOutCubic),
  );
  for (const h of hlDecl) {
    yield h.rect.size.x(h.w, HL_DUR, easeInOutSine);
    yield* waitFor(HL_STEP);
  }
  yield* waitFor(READ_DECL);

  // ═══ 7. Печатная строка → наш код НА ТОМ ЖЕ МЕСТЕ ═══════════════════
  // ⚠️ Буквы НЕ морфим: машинописные и наши глифы слишком разной формы.
  // Связь держит то, что текст не двигается и не меняет кегль.
  graph().opacity(1);
  declNodes.forEach(n => n.opacity(0));
  yield* all(
    ...declNodes.map(n => n.opacity(1, 0.75, easeOutCubic)),
    rh9().opacity(0, 0.85, easeInOutCubic),
    rh9Blur(6, 0.85, easeInOutCubic),
  );
  rh9().position(rigAt(AT_PARTIAL, RH_READ_S));
  rh9Blur(0);
  yield* waitFor(0.5);

  // ═══ 8. Код перестраивается в столбец ═══════════════════════════════
  // ⚠️ Здесь же графит садится на свой рабочий масштаб GRAPH_S: до этого
  // момента он обязан стоять 1:1 с машинописью, а здесь и так всё едет —
  // ужатие прячется в перестроении и не читается отдельным событием.
  yield* all(
    graph().scale(GRAPH_S, REFLOW, easeInOutCubic),
    ...declNodes.map((n, i) => n.position(new Vector2(DECL[i].cl[0], DECL[i].cl[1]), REFLOW, easeInOutCubic)),
    ...dupNodes.map(d => d.node.position(new Vector2(d.to[0], d.to[1]), REFLOW, easeInOutCubic)),
    ...dupNodes.map(d => chain(waitFor(REFLOW * 0.3), d.node.opacity(1, REFLOW * 0.5, easeOutCubic))),
  );
  yield* waitFor(0.4);

  // ═══ 9. Карточка растёт ИЗ кода, по одному полю ═════════════════════
  // ⚠️ Соответствие показываем ВЫСОТОЙ: строка N и клетка N на одной
  // базовой линии. Читаемое поле горит, остальные приглушены.
  const focusRow = (active: number, dur: number) =>
    all(
      ...rowNodes.flatMap((rn, r) =>
        rn.map(n => n.opacity(active < 0 || r === active ? 1 : 0.28, dur, easeInOutCubic)),
      ),
    );
  yield* cardLabel.opacity(1, CARD_IN, easeOutCubic);
  for (let i = 0; i < 5; i++) {
    yield* all(focusRow(i + 1, 0.35), cells[i].opacity(1, 0.4, easeOutCubic));
    yield* waitFor(FIELD_STEP);
  }
  yield* focusRow(-1, 0.5);

  // ═══ 10. Каскад строк создания ══════════════════════════════════════
  const caretBlink = function* (total: number) {
    makeCaret.opacity(1);
    let t = 0;
    while (t + 0.54 <= total) {
      yield* waitFor(0.3);
      makeCaret.opacity(0);
      yield* waitFor(0.24);
      makeCaret.opacity(1);
      t += 0.54;
    }
    yield* waitFor(Math.max(0, total - t));
    makeCaret.opacity(0);
  };
  makeRow().opacity(1);
  const typedIn = [0, 0, 0];
  for (let i = 0; i < MAKE.length; i++) {
    const tok = MAKE[i];
    if (tok.stall) {
      makeCaret.position.x(typedIn[tok.line] * CODE_ADV);
      makeCaret.position.y(tok.line * MAKE_PITCH);
      yield* caretBlink(tok.stall);
    }
    for (let c = 1; c <= tok.t.length; c++) {
      makeNodes[i].text(tok.t.slice(0, c));
      typedIn[tok.line]++;
      yield* waitFor(MAKE_CHAR);
    }
    if (tok.cell) yield* tok.cell.opacity(1, 0.28, easeOutCubic);
    // точка-ссылка в пятой клетке — в момент печати аргумента
    if (tok.dot) yield* tok.dot.opacity(1, 0.3, easeOutCubic);
    if (tok.act === 'cardK') {
      // скобка закрыта — и только теперь стрелка ведёт к новой записи
      yield* all(
        arrow.end(1, ARROW_DRAW, easeInOutCubic),
        chain(waitFor(0.15), other().opacity(1, 0.5, easeOutCubic)),
      );
      yield* waitFor(K_HOLD);
    }
    if (tok.act === 'cardJ') {
      // вторая скобка закрыта — семья отъезжает, и приходит третья запись
      yield* all(
        graph().scale(FAM_S, FAM_D, easeInOutCubic),
        graph().position(new Vector2(FAM_POS[0], FAM_POS[1]), FAM_D, easeInOutCubic),
        chain(
          waitFor(FAM_D * 0.35),
          all(
            arrow3.end(1, ARROW_DRAW, easeInOutCubic),
            chain(waitFor(0.15), card3().opacity(1, 0.5, easeOutCubic)),
          ),
        ),
      );
      yield* waitFor(FAM_HOLD * 0.5);
    }
  }
  yield* waitFor(PAYOFF_HOLD);

  // ═══ 11. МОРФ в две фазы: центрирование → кроссфейд ═════════════════
  // (A) Окружение гаснет, а карточка СНАЧАЛА встаёт на место ящика 1908:
  // клетки едут и растягиваются, кадр возвращается из семейного отъезда.
  // Страницы ещё нет — на графите остаётся одна карточка в центре.
  const recW = (BOX_1908.x1 - BOX_1908.x0) * DIAG_S_BOX;
  const recH = (BOX_1908.y1 - BOX_1908.y0) * DIAG_S_BOX;
  const recCellH = recH / 5;
  const recCellCY = (i: number) => -recH / 2 + recCellH * (i + 0.5);
  yield* all(
    graph().scale(GRAPH_S, MORPH_A, easeInOutCubic),
    graph().position(new Vector2(0, 0), MORPH_A, easeInOutCubic),
    ...declNodes.map(n => n.opacity(0, MORPH_A * 0.5, easeInCubic)),
    ...dupNodes.map(d => d.node.opacity(0, MORPH_A * 0.5, easeInCubic)),
    other().opacity(0, MORPH_A * 0.5, easeInCubic),
    card3().opacity(0, MORPH_A * 0.5, easeInCubic),
    arrow.opacity(0, MORPH_A * 0.5, easeInCubic),
    arrow3.opacity(0, MORPH_A * 0.5, easeInCubic),
    dot3.opacity(0, MORPH_A * 0.5, easeInCubic),
    makeRow().opacity(0, MORPH_A * 0.5, easeInCubic),
    cardLabel.opacity(0, MORPH_A * 0.5, easeInCubic),
    ...cells.map((c, i) =>
      all(
        c.position(new Vector2(0, recCellCY(i)), MORPH_A, easeInOutCubic),
        c.size([recW, recCellH], MORPH_A, easeInOutCubic),
      ),
    ),
    ...[val1908, valTrue, cellNulls[0], cellNulls[1]].map((v, i) =>
      v.position(new Vector2(0, recCellCY(i)), MORPH_A, easeInOutCubic),
    ),
    arrowDot.position(new Vector2(0, recCellCY(4)), MORPH_A, easeInOutCubic),
  );
  yield* waitFor(0.25);

  // (B) КРОССФЕЙД: страница проявляется. ⚠️ КОНТУР ГАСНЕТ ПЕРВЫМ И СРАЗУ
  // (за 35% фазы, без чернильного перекраса — он и был «задержавшейся
  // рамкой», дважды правка автора); значения темнеют до чернил и передают
  // себя рукописи чуть дольше — они и есть эстафета.
  yield* all(
    rh11().opacity(1, MORPH_B, easeInOutCubic),
    ...cells.map(c => c.opacity(0, MORPH_B * 0.35, easeInCubic)),
    ...[val1908, valTrue, cellNulls[0], cellNulls[1]].map(v =>
      all(
        v.fill(MORPH_INK, MORPH_B * 0.3, easeInOutCubic),
        chain(waitFor(MORPH_B * 0.15), v.opacity(0, MORPH_B * 0.4, easeInCubic)),
      ),
    ),
    all(
      arrowDot.fill(MORPH_INK, MORPH_B * 0.3, easeInOutCubic),
      chain(waitFor(MORPH_B * 0.15), arrowDot.opacity(0, MORPH_B * 0.4, easeInCubic)),
    ),
  );
  graph().remove();
  yield* waitFor(0.4);

  // ═══ 12. Его null'ы в записи 1908 ═══════════════════════════════════
  for (const n of boxNulls) {
    yield* n.rect.size.x(n.w, HL_DUR * 0.7, easeInOutSine);
  }
  yield* waitFor(BOX_HL_HOLD);

  // ═══ 13. Отъезд: вся семья его рукой — вот и остальные person ═══════
  yield* all(
    rh11().scale(DIAG_S_WIDE * GRAPH_S, DIAG_BACK, easeInOutCubic),
    rh11().position(rigAt(DIAG_AT_WIDE, DIAG_S_WIDE * GRAPH_S), DIAG_BACK, easeInOutCubic),
  );
  for (const n of restNulls) {
    yield n.rect.size.x(n.w, HL_DUR * 0.55, easeInOutSine);
    yield* waitFor(REST_STEP);
  }
  yield* waitFor(DIAG_HOLD);

  // ═══ 14. Чертёж ОТКЛАДЫВАЮТ — под ним стр. 9 ═══════════════════════
  // Тот же жест, что с листом ALGOL: страница уезжает влево-вниз с
  // поворотом, под ней уже лежит текст. Физическая склейка, не растворение.
  rh9().opacity(1);
  yield* all(
    rh11().position.x(rh11().position.x() - 3300, P11_ASIDE, easeInCubic),
    rh11().position.y(rh11().position.y() + 320, P11_ASIDE, easeInCubic),
    rh11().rotation(-9, P11_ASIDE, easeInCubic),
  );
  rh11().opacity(0);
  rh11().remove();
  yield* waitFor(0.35);
  yield* hlPartial.rect.size.x(hlPartial.w, HL_DUR, easeInOutSine);
  yield* waitFor(READ_PARTIAL);
  yield* rh9().position(rigAt(AT_NULL, RH_READ_S), 0.85, easeInOutCubic);
  yield* hlNullA.rect.size.x(hlNullA.w, HL_DUR, easeInOutSine);
  yield* hlNullB.rect.size.x(hlNullB.w, HL_WRAP, easeInOutSine);
  yield* waitFor(READ_NULL);

  // ═══ 15. Его признание — встык с его печатным решением ══════════════
  yield* all(
    rh9Blur(7, EASY_IN, easeInOutCubic),
    rh9Scrim.opacity(0.62, EASY_IN, easeInOutCubic),
    easy().opacity(1, EASY_IN, easeOutCubic),
    easyBlur(0, EASY_IN, easeOutCubic),
  );
  yield* waitFor(EASY_HOLD);

  yield rh9Blur(10, SQUEEZE_DUR, easeInOutCubic);
  yield* frameH(BAND_H, SQUEEZE_DUR, easeInOutCubic);
  yield* waitFor(BAND_HOLD);

  // ═══ 16. Лента языков в полосе: чем это лечили ══════════════════════
  // ⚠️ СТРОГО ПОСЛЕ цитаты: роза гаснет ПОЛНОСТЬЮ, пауза, и только потом
  // лента — одновременный вход накладывал Kotlin на цитату.
  yield* easy().opacity(0, 0.45, easeInCubic);
  easy().remove();
  yield* waitFor(0.2);
  belt().position.x(-pairCenters[0]);
  yield* belt().opacity(1, BELT_IN, easeOutCubic);
  yield* waitFor(BELT_READ);
  for (let i = 1; i < LANGS.length; i++) {
    yield* all(
      belt().position.x(-pairCenters[i], BELT_STEP, easeInOutCubic),
      ...pairOps.map((op, j) => op(j === i ? 1 : BELT_DIM, BELT_STEP, easeInOutCubic)),
    );
    yield* waitFor(BELT_READ);
  }
  yield* waitFor(0.4);

  shutterTop.position.y(-(BAND_H / 2 + SHUT_H / 2));
  shutterBot.position.y(BAND_H / 2 + SHUT_H / 2);
  yield* all(
    shutterTop.position.y(-SHUT_H / 2, SHUT_DUR, easeInOutCubic),
    shutterBot.position.y(SHUT_H / 2, SHUT_DUR, easeInOutCubic),
  );
  yield* waitFor(0.5);
});
