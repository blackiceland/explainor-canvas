// Hydra — примитивы точечных полей на three.js внутри Motion Canvas.
//
// Много голов — много точек. Это НЕ генератор сцен: композиция, такты и
// параметры рельефа у каждой сцены свои, и вся ценность сцены именно там.
// Здесь только то, что уже было написано дважды и дорого далось:
//
//   noise      — детерминированный шум (mulberry32, value noise, ridged)
//   PointField — поле плотности: материал + сэмплер поля высот + экспозиция
//   Ribbon     — нити постоянной экранной ширины (не LineSegments2) и искры
//   Flashes    — расписание вспышек и правило «третья не гаснет»
//   DollyRig   — камера одним движением: отход с подъёмом и сужением угла
//
// Всё — чистая функция сигналов MC: сцены обязаны скраббиться.

export * from './noise';
export * from './PointField';
export * from './Ribbon';
export * from './Flashes';
export * from './DollyRig';
