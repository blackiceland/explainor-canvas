# 🗺️ Roadmap v2: Explainor Studio — от личного инструмента к SaaS

## 📋 Оглавление

1. Принципы планирования
2. Phase 1: Component Library v0 (Weeks 1–5)
3. Phase 2: Operators + Shot Runtime (Weeks 6–9)
4. Phase 3: Semantic IR-Driven (Weeks 10–20)
5. Phase 4: Production SaaS (Weeks 21+)
6. Рабочий workflow с AI

---

## 🧭 Принципы планирования

Главная цель — начать выпускать ролики как можно раньше. Поэтому первая фаза ориентирована на твои ближайшие YouTube-темы, а не на «покрыть весь тех-мир сразу». Любая новая сущность попадает в ядро только после того, как она реально понадобилась в сцене.

Архитектура растёт снизу вверх, но базовые границы мы не нарушаем с первого дня: доменная часть не наследуется от Motion Canvas, семантика сцены отделена от вычисленной геометрии, шоты — внутренний механизм, а публичный язык — операторы и IR.

Детерминизм обязателен: один и тот же IR при той же версии рендера должен давать тот же результат. Поэтому headless-рендер как программный API появится раньше, чем рендер-ферма.

---

## 🟢 Phase 1: Component Library v0 для себя (Weeks 1–5)

### Цель фазы

Собрать минимальное, но качественное ядро 2D-компонентов и сцен под твои реальные ролики, не закладывая технический долг в фундамент.

### Критерии успеха

Ты стабильно делаешь короткие вставки в видео (30–90 секунд) быстрее, чем вручную. Компоненты имеют единый стиль и повторяемые паттерны анимаций. Каркас уже совместим с будущим IR-подходом.

---

### Week 1: Каркас рендера и доменные компоненты

Сначала фиксируем правильную базу, чтобы потом не ломать API.

Задачи недели: создать `RenderContext` как фабрику view-объектов, базовый `AnimatedComponent` без наследования от `Node`, `Theme` и минимальный набор примитивов (box, text, arrow). Всё это живёт в `motion-canvas/src/core` и используется в ручных сценах.

Мини-пример (текущая реализация v0).

```ts
export interface ColorTheme {
  background: string
  surface: string
  text: {
    primary: string
    secondary: string
    code: string
  }
  stroke: {
    primary: string
    secondary: string
  }
  status: {
    success: string
    warning: string
    error: string
    info: string
  }
}

export interface Theme {
  colors: ColorTheme
  fonts: {
    primary: string
    code: string
  }
  spacing: {
    small: number
    medium: number
    large: number
  }
  timing: {
    fast: number
    medium: number
    slow: number
  }
}

export class RenderContext {
  constructor(
    public view: any,
    public theme: Theme,
  ) {}

  createRect(spec: any): any {}
  createText(spec: any): any {}
  createLine(spec: any): any {}
  createGroup(spec: any): any {}
}

export abstract class AnimatedComponent {
  protected theme: Theme | null = null

  public mount(ctx: RenderContext): void {
    this.theme = ctx.theme
    this.onMount(ctx)
  }

  protected abstract onMount(ctx: RenderContext): void

  protected getTiming(duration?: number): number {
    if (duration !== undefined) {
      return duration
    }
    if (this.theme === null) {
      throw new Error('Theme is not initialized. Call mount(ctx) before using timing.')
    }
    return this.theme.timing.medium
  }

  abstract appear(duration?: number): Promise<void>
}
```

Результат недели: у тебя есть «правильный фундамент» в `motion-canvas`, и хотя бы один тестовый демо-шот, который рендерится локально из кода без участия веб-UI.

---

### Weeks 2–3: Домен 1 под твой контент

Выбираешь один ближайший контент-домен (например Kafka или Postgres), и делаешь его «глубоко», а не «по верхам».

За две недели: 6–8 компонентов домена и 2–3 сцены. Примеры: для Kafka — producer, topic, consumer group, message token, rebalance highlight; для Postgres — table, index, query, join flow, replication.

Делаешь одну «витринную» сцену, которая точно пойдёт в ролик и заставит ядро пройти реальную нагрузку.

---

### Weeks 4–5: Второй домен и стабилизация библиотеки

Добавляешь второй домен под следующие ролики. Тут уже быстрее, потому что каркас и стиль сформированы.

Параллельно делаешь чистку: вытаскиваешь повторяющиеся паттерны появления/подсветки в утилиты, приводишь пропсы и темы к единому виду, заводишь первые golden-тесты на кадры двух-трёх сцен.

2.5D в этой фазе не цель. Если ты хочешь — делай мини-эксперимент отдельно, но не в счёт прогресса v0.

---

## 🟡 Phase 2: Семантические операторы и Shot runtime (Weeks 6–9)

### Цель фазы

Перейти от «ручных монолитных сцен» к семантическим планам, которые компилируются в low-level шоты. Это основа будущего IR и AI-режиссёра.

### Критерий успеха

Новая сцена пишется как список операторов (в коде), а не как длинная покадровая функция. Шоты перестают быть публичным API.

---

### Week 6: Low-level Shots + TimelinePlayer

Делаешь небольшой набор шотов: appear, disappear, highlight, camera pan/zoom, wait, parallel/sequence. Это чистый runtime.

---

### Week 7: Ввод Operators-слоя

Фиксируешь архитектурную границу: Operator → Shots.

Начинаешь с 6–8 операторов, которые ты регулярно используешь в сценах. Примерный минимум: introduce nodes/edges, zoomTo, emphasize, travelToken, compareStates, explodeCluster.

---

### Weeks 8–9: Рефакторинг сцен на Operators

Переписываешь 4–6 существующих сцен через операторы и закрываешь пробелы в библиотеке. Именно тут у тебя появляются «правильные» составные операторы вроде ShowRequestPath или RevealCluster, которые внутри порождают несколько шотов.

Фаза заканчивается тем, что ты можешь строить новые сцены очень коротко и стабильно.

---

## 🟠 Phase 3: Semantic IR-Driven (Weeks 10–20)

### Цель фазы

Сделать внешний контракт сцен декларативным: семантические сущности + список операторов. Сцены перестают быть TS-кодом на входе.

### Критерии успеха

IR/JSON → видео. Старые сцены рендерятся через GenericScene. Backend хранит, валидирует и версионирует IR.

---

### Weeks 10–11: Проектирование Semantic IR

Ты фиксируешь v1.0 IR в форме
`entities` (что есть в сцене) + `operators` (что с этим делаем) + `style`.

Координаты на этом уровне либо отсутствуют, либо разрешены только в ручном режиме v0.

---

### Weeks 12–13: IR-фабрики в render-plane

ElementFactory создаёт доменные компоненты из entities. OperatorFactory создаёт операторы. GenericScene начинает исполнять IR и вычисленный layout.

---

### Weeks 14–16: Backend v1 (control plane)

Spring Boot-слой: хранение IR проектов/сцен, схемная валидация, версии, миграции. Пока без UI и без многопользовательщины.

Тут же заводишь «IR-migration pipeline»: при чтении старый IR автоматически переводится в новый.

---

### Weeks 17–18: Конвертация библиотечного наследия

Переводишь основные сцены на IR-формат. Остаётся один GenericScene.

Параллельно усиливаешь golden-тесты на уровне IR → кадр.

---

### Weeks 19–20: Мини-планировщик времени

Вводишь deterministic scheduler как отдельный модуль: он принимает список операторов и отношения времени (пока простые), выдаёт тайминги. Это подготовка к аудио-синху и AI.

После этого ты уже живёшь в мире IR на входе без потери скорости для себя.

---

## 🔴 Phase 4: Production SaaS (Weeks 21+)

### Цель фазы

Перевести систему в облачный режим: авто-лейаут, headless-рендер, очередь задач и UI.

### Критерий успеха

Пользователь отправляет IR/JSON и получает mp4 по job-id. Система масштабируется и кэширует повторные рендеры.

---

### Weeks 21–22: Headless Render API

Делаешь чистый программный рендер в контейнере:
IR → computed layout → video без участия UI и кликов.

Это должен быть стабильный CLI/Node-entrypoint, который потом вызывают воркеры.

---

### Weeks 23–25: Layout Layer v1

Сначала простой grid/flow layout как fallback, затем подключение ELKjs для графов. Главное — computed layout возвращается отдельным артефактом и не мутирует semantic IR.

---

### Weeks 26–28: Render Queue + Workers

Backend кладёт задачи в очередь (Redis). Воркеры вызывают headless-рендер, складывают результат в S3/MinIO. Добавляешь кэш по hash(semantic IR + layout version + theme + audio).

---

### Weeks 29–30: Web UI v1

Минимальный UI: проекты, загрузка IR, предпросмотр, статус рендера, библиотека шаблонов. Это «коммерческий скелет», дальше он уже растёт органически.

---

## 🤖 Workflow с AI

Ты работаешь по циклу «сцена → компоненты → оператор → IR».

Сначала формулируешь сцену словами, и просишь AI накидать нужные компоненты/операторы под неё. Затем ты рендеришь, смотришь, что реально не хватает, и только после этого закрепляешь новое в библиотеке. Любая большая идея должна пройти через реальный ролик, иначе она не попадает в ядро.

На Phase 3 AI может помогать сразу генерировать IR (entities + operators). Но финальная правка и адаптация — всегда через твой детерминированный слой и тесты.

---

Если идти по этому роадмапу, ты получаешь рабочий личный инструмент уже в первые недели, а затем без перелома архитектуры приходишь к IR-контракту, планировщику, автолейауту и SaaS-рендеру. Основные риски, которые были в прошлой версии — ранняя привязка к Motion Canvas, шот-центричный IR и хрупкий прод-рендер через UI-клики — здесь убраны.
