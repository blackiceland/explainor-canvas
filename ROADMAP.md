# 🗺️ Roadmap: От простого к сложному

## 📋 Оглавление
1. [Принципы планирования](#принципы-планирования)
2. [Phase 1: Component Library (Weeks 1-4)](#phase-1-component-library)
3. [Phase 2: Scene Composition (Weeks 5-8)](#phase-2-scene-composition)
4. [Phase 3: JSON-Driven (Weeks 9-16)](#phase-3-json-driven)
5. [Phase 4: Production (Weeks 17-24+)](#phase-4-production)
6. [Workflow: Как работать с AI](#workflow-как-работать-с-ai)

---

## 🧭 Принципы планирования

### 1. MVP как можно скорее
**Цель:** Ты должен начать создавать видео **через 1-2 недели**, а не через 3 месяца.

### 2. Правило 70/30
- **70-90% времени:** 2D-компоненты (простые, читаемые, быстро делать)
- **10-30% времени:** 2.5D изометрия (визуальные акценты)

### 3. Работа по запросу
**Ты:** "Сделай сцену с Kafka producer → topic → consumer"  
**AI:** Создаёт компоненты + сцену  
**Ты:** Рендеришь → получаешь MP4 → используешь в видео

### 4. Архитектура "растёт снизу вверх"
Не проектируем всё заранее. Пишем компоненты → замечаем паттерны → рефакторим.

### 5. Маленькие шаги, частые релизы
Каждая неделя = рабочий результат (новые компоненты → новые сцены → новые видео).

---

## 🟢 Phase 1: Component Library (Weeks 1-4)

### Цель фазы
Библиотека из 15-20 переиспользуемых компонентов для ручного создания сцен.

### Критерий успеха
✅ 5-7 готовых сцен для видео  
✅ Компоненты следуют единому стилю  
✅ Архитектурные паттерны установлены

---

### 📅 Week 1: Kafka Components (Базовая инфраструктура)

**Задачи:**

#### 1.1. Базовая инфраструктура (Day 1-2)
- [ ] Создать `AnimatedComponent` (базовый класс)
- [ ] Создать `Theme` (единый стиль)
- [ ] Создать `Renderer2D` (плоский рендеринг)
- [ ] Настроить структуру папок (`components/kafka/`, `scenes/kafka/`)

**Результат:** Фундамент для всех компонентов.

**Код:**
```typescript
// components/base/AnimatedComponent.ts
export abstract class AnimatedComponent extends Node {
  abstract appear(duration?: number): Promise<void>;
  abstract disappear(duration?: number): Promise<void>;
  abstract highlight(color?: string): Promise<void>;
}

// styles/DefaultTheme.ts
export const DefaultTheme: Theme = {
  colors: { primary: '#3B82F6', secondary: '#10B981', accent: '#F59E0B' },
  // ...
};
```

---

#### 1.2. Kafka Components (Day 3-5)
- [ ] `KafkaTopic` (прямоугольник с названием + партиции)
- [ ] `KafkaProducer` (клиент с иконкой)
- [ ] `KafkaConsumer` (клиент с иконкой)
- [ ] `KafkaMessage` (маленький прямоугольник с payload)
- [ ] `Arrow` (стрелка между компонентами)

**Результат:** 5 базовых Kafka-компонентов.

**Примеры использования:**
```typescript
const topic = <KafkaTopic 
  x={0} 
  y={0} 
  name="user-events" 
  partitions={3} 
/>;

const producer = <KafkaProducer 
  x={-200} 
  y={0} 
  name="user-service" 
/>;

const arrow = <Arrow 
  from={producer} 
  to={topic} 
  animated={true} 
/>;
```

---

#### 1.3. Первая сцена (Day 6-7)
- [ ] Создать `kafkaProducerConsumerScene.tsx`
- [ ] Producer → Topic → Consumer с анимацией
- [ ] Сообщение "летит" от producer к consumer
- [ ] Экспорт MP4

**Результат:** Готовое видео для YouTube (30-60 секунд).

**Пример запроса к AI:**
> "Сделай сцену: user-service отправляет сообщение в топик user-events, затем analytics-service читает это сообщение. Покажи анимацию полёта сообщения."

---

### 📅 Week 2: Database Components

**Задачи:**

#### 2.1. Database Components (Day 1-4)
- [ ] `Table` (таблица с колонками)
- [ ] `Row` (строка данных)
- [ ] `Index` (B-tree визуализация, упрощённая)
- [ ] `Query` (SQL-запрос с планом выполнения)
- [ ] `Join` (визуализация JOIN'а двух таблиц)

**Результат:** 5 DB-компонентов.

**Примеры:**
```typescript
const usersTable = <Table 
  x={-200} 
  y={0} 
  name="users" 
  columns={['id', 'name', 'email']} 
/>;

const ordersTable = <Table 
  x={200} 
  y={0} 
  name="orders" 
  columns={['id', 'user_id', 'amount']} 
/>;

const join = <Join 
  left={usersTable} 
  right={ordersTable} 
  on="users.id = orders.user_id" 
/>;
```

---

#### 2.2. Database сцены (Day 5-7)
- [ ] `sqlJoinScene.tsx` — визуализация JOIN
- [ ] `indexScanScene.tsx` — сканирование по индексу vs full scan
- [ ] `transactionScene.tsx` — ACID (упрощённая версия)

**Результат:** 3 готовых сцены для видео про SQL.

**Пример запроса к AI:**
> "Сделай сцену: SELECT * FROM users JOIN orders ON users.id = orders.user_id. Покажи, как строки из users соединяются со строками из orders."

---

### 📅 Week 3: Network & Algorithm Components

**Задачи:**

#### 3.1. Network Components (Day 1-3)
- [ ] `Server` (сервер с иконкой)
- [ ] `Client` (клиент с иконкой)
- [ ] `Request` (HTTP-запрос с методом + путём)
- [ ] `Response` (ответ с status code)
- [ ] `LoadBalancer` (визуализация балансировки)

**Результат:** 5 сетевых компонентов.

---

#### 3.2. Algorithm Components (Day 4-7)
- [ ] `Array` (массив с индексами)
- [ ] `LinkedList` (связный список с указателями)
- [ ] `Tree` (бинарное дерево)
- [ ] `Graph` (граф с вершинами и рёбрами)
- [ ] `Pointer` (стрелка-указатель для алгоритмов)

**Результат:** 5 алгоритмических компонентов.

**Примеры:**
```typescript
const array = <Array 
  x={0} 
  y={0} 
  values={[5, 2, 8, 1, 9]} 
  highlightIndex={2} 
/>;

const tree = <Tree 
  x={0} 
  y={0} 
  root={{ value: 10, left: {...}, right: {...} }} 
/>;
```

**Сцены:**
- [ ] `binarySearchScene.tsx` — бинарный поиск в массиве
- [ ] `linkedListInsertScene.tsx` — вставка в связный список
- [ ] `treeTraversalScene.tsx` — обход дерева (in-order)

---

### 📅 Week 4: Kubernetes Components & Изометрия

**Задачи:**

#### 4.1. Kubernetes Components (Day 1-3)
- [ ] `Pod` (контейнеры внутри)
- [ ] `Service` (load balancer перед подами)
- [ ] `Deployment` (несколько подов)
- [ ] `Ingress` (маршрутизация трафика)

**Результат:** 4 K8s-компонента.

---

#### 4.2. Изометрия (2.5D) — первые эксперименты (Day 4-7)
- [ ] Рефакторинг `domino2dProtoScene` → `DominoComponent`
- [ ] Создать `RendererIso` (изометрический рендерер)
- [ ] Один K8s-компонент в изометрии (например, `Pod`)

**Результат:** 
- Изометрия работает
- Понимание, где использовать 2.5D (акценты, а не всё подряд)

**Правило 70/30:**
- 70% сцен — плоский 2D (читаемость)
- 30% сцен — изометрия (визуальные акценты, "вау-эффект")

---

### 🎯 Итоги Phase 1 (Week 1-4)

**Что получили:**
- ✅ 20+ компонентов (Kafka, DB, Network, Algorithm, K8s)
- ✅ 10+ готовых сцен
- ✅ Единый визуальный стиль (Theme)
- ✅ Архитектурные паттерны (AnimatedComponent, Renderer, Signals)
- ✅ 2D + изометрия (70/30 правило)

**Что можешь делать:**
- ✅ Создавать YouTube-ролики про Kafka, SQL, алгоритмы, K8s
- ✅ Запрашивать у AI новые компоненты ("сделай Redis pub/sub")
- ✅ Комбинировать компоненты в новые сцены

**Что НЕ умеем:**
- ❌ JSON → видео (всё ещё пишем .tsx вручную)
- ❌ Авто-лейаут (координаты задаём руками)
- ❌ Backend API (нет)

---

## 🟡 Phase 2: Scene Composition (Weeks 5-8)

### Цель фазы
Переход от монолитных сцен к композиции из микро-шотов (shots).

### Зачем это нужно?
- ✅ Переиспользование шотов (один шот = много сцен)
- ✅ Упрощение тестирования (тестируем шоты, а не сцены)
- ✅ Подготовка к JSON-driven (шоты = JSON-сериализуемы)

---

### 📅 Week 5: Shot Infrastructure

**Задачи:**

#### 5.1. Базовые шоты (Day 1-3)
- [ ] `Shot` (абстрактный класс)
- [ ] `ComponentAppearShot` (появление компонента)
- [ ] `ComponentDisappearShot` (исчезновение)
- [ ] `HighlightShot` (подсветка компонента)
- [ ] `CameraPanShot` (перемещение камеры)

**Код:**
```typescript
// shots/Shot.ts
export abstract class Shot {
  abstract execute(view: View2D): Promise<void>;
}

// shots/ComponentAppearShot.ts
export class ComponentAppearShot extends Shot {
  constructor(private component: AnimatedComponent) { }
  
  async execute(view: View2D) {
    view.add(this.component);
    await this.component.appear();
  }
}
```

---

#### 5.2. Timeline Player (Day 4-5)
- [ ] `Timeline` (последовательность шотов)
- [ ] `TimelinePlayer` (проигрыватель)

**Код:**
```typescript
// core/Timeline.ts
export class Timeline {
  constructor(private shots: Shot[]) { }
  
  async *play(view: View2D) {
    for (const shot of this.shots) {
      yield* shot.execute(view);
    }
  }
}
```

---

#### 5.3. Рефакторинг существующих сцен (Day 6-7)
- [ ] Переписать 3-5 сцен из Week 1-4 через шоты
- [ ] Сравнить "до/после" (читаемость кода)

**До (монолитная сцена):**
```typescript
export default makeScene2D(function* (view) {
  const topic = <KafkaTopic x={0} y={0} name="events" />;
  view.add(topic);
  yield* topic.appear();
  yield* topic.highlight('#FFD700');
  // ... 50 строк
});
```

**После (shot-based):**
```typescript
export default makeScene2D(function* (view) {
  const topic = <KafkaTopic x={0} y={0} name="events" />;
  
  const timeline = new Timeline([
    new ComponentAppearShot(topic),
    new HighlightShot(topic, '#FFD700'),
  ]);
  
  yield* timeline.play(view);
});
```

---

### 📅 Week 6-7: Доп. шоты + новые сцены

**Задачи:**

#### 6.1. Дополнительные шоты (Day 1-5)
- [ ] `TextOverlayShot` (наложение текста)
- [ ] `ArrowDrawShot` (анимация рисования стрелки)
- [ ] `CameraZoomShot` (zoom камеры)
- [ ] `WaitShot` (пауза)
- [ ] `ParallelShot` (несколько шотов параллельно)

---

#### 6.2. Сложные сцены (Day 6-10)
- [ ] `kafkaRebalanceScene` (Kafka consumer group rebalance)
- [ ] `dbReplicationScene` (Master-Slave репликация)
- [ ] `k8sRollingUpdateScene` (Rolling update подов)

**Результат:** Понимание, какие шоты нужны чаще всего.

---

### 📅 Week 8: Извлечение паттернов

**Задачи:**

#### 8.1. Анализ и рефакторинг (Day 1-4)
- [ ] Найти дублирующиеся паттерны в шотах
- [ ] Извлечь базовые классы (`AnimatedShot`, `CameraShot`)
- [ ] Создать утилиты (`sequence()`, `parallel()`)

---

#### 8.2. Документация (Day 5-7)
- [ ] Написать `SHOT_GUIDE.md` (как создавать шоты)
- [ ] Примеры 10+ типовых шотов
- [ ] Best practices

---

### 🎯 Итоги Phase 2 (Week 5-8)

**Что получили:**
- ✅ 15+ базовых шотов
- ✅ Shot-based архитектура (вместо монолитных сцен)
- ✅ Переиспользование (один шот = много сцен)
- ✅ Упрощённое тестирование

**Готовность к Phase 3:**
- ✅ Шоты легко сериализуются в JSON
- ✅ Понимание, какие абстракции нужны

---

## 🟠 Phase 3: JSON-Driven (Weeks 9-16)

### Цель фазы
Генерация сцен из JSON (без написания кода).

### Критерий успеха
✅ POST JSON → GET MP4  
✅ Backend генерирует JSON динамически  
✅ GenericScene читает JSON и рендерит

---

### 📅 Week 9-10: Scene IR (Intermediate Representation)

**Задачи:**

#### 9.1. JSON Schema (Day 1-3)
- [ ] Определить JSON-схему для Scene IR
- [ ] Валидация (JSON Schema validator)
- [ ] Версионирование (v1.0)

**Пример IR:**
```json
{
  "version": "1.0",
  "sceneId": "kafka-producer-consumer",
  "renderMode": "2d",
  "background": "#F5F5F5",
  "shots": [
    {
      "id": "shot-1",
      "type": "component-appear",
      "component": {
        "type": "KAFKA_TOPIC",
        "props": { "name": "events", "x": 0, "y": 0, "partitions": 3 }
      },
      "duration": 0.5
    }
  ]
}
```

---

#### 9.2. Фабрики (Day 4-7)
- [ ] `ElementFactory` (создание компонентов из JSON)
- [ ] `ShotFactory` (создание шотов из JSON)
- [ ] `TimelineFactory` (построение timeline из JSON)

**Код:**
```typescript
// core/ElementFactory.ts
export class ElementFactory {
  static create(def: ElementDefinition): AnimatedComponent {
    switch (def.type) {
      case 'KAFKA_TOPIC': return new KafkaTopic(def.props);
      case 'KAFKA_PRODUCER': return new KafkaProducer(def.props);
      // ...
    }
  }
}
```

---

#### 9.3. GenericScene (Day 8-10)
- [ ] Создать `GenericScene.tsx` (универсальная сцена)
- [ ] Интеграция с `animationClient.ts` (fetch JSON)
- [ ] Тестирование на существующих сценах

**Код:**
```typescript
// scenes/GenericScene.tsx
export default makeScene2D(function* (view) {
  const sceneId = getSceneIdFromURL();
  const sceneData = yield fetchScene(sceneId);
  
  view.fill(sceneData.background);
  
  const timeline = TimelineFactory.fromJSON(sceneData);
  yield* timeline.play(view);
});
```

---

### 📅 Week 11-12: Backend API

**Задачи:**

#### 11.1. Scene Domain (Day 1-4)
- [ ] `Scene` (агрегат)
- [ ] `Shot` (entity)
- [ ] `Element` (value object)
- [ ] `SceneRepository` (интерфейс)

**Java код:**
```java
// domain/animation/Scene.java
public class Scene {
    private final SceneId sceneId;
    private final String name;
    private final List<Shot> shots;
    private final RenderMode renderMode;
    
    public SceneResponse toResponse() {
        // Маппинг в DTO
    }
}
```

---

#### 11.2. REST API (Day 5-7)
- [ ] `SceneController` (GET /api/v1/scenes/{id})
- [ ] `SceneAssembler` (Domain → DTO)
- [ ] Тестирование (Postman / curl)

---

#### 11.3. Scene Builders (Day 8-10)
- [ ] `KafkaSceneBuilder` (генерирует Kafka-сцены)
- [ ] `DatabaseSceneBuilder` (генерирует DB-сцены)
- [ ] Фабрика билдеров

**Java код:**
```java
// domain/animation/builders/KafkaSceneBuilder.java
public class KafkaSceneBuilder implements SceneBuilder {
    
    public Scene buildProducerConsumer(String topicName) {
        return Scene.builder()
            .addShot(Shot.componentAppear(
                Element.kafkaTopic(topicName, 3)
            ))
            .build();
    }
}
```

---

### 📅 Week 13-14: Интеграция Frontend + Backend

**Задачи:**

#### 13.1. Конвертация существующих сцен (Day 1-5)
- [ ] Конвертировать 5-7 сцен в JSON (через backend)
- [ ] Проверить идентичность (старая сцена vs JSON-driven)

---

#### 13.2. Добавление новых сцен (Day 6-10)
- [ ] Реализовать 3-5 новых сцен **только через JSON** (не трогая frontend)
- [ ] Проверить, что GenericScene их правильно рендерит

**Критерий успеха:** Новая сцена = только backend-код (0 строк frontend).

---

### 📅 Week 15-16: Миграции и рефакторинг

**Задачи:**

#### 15.1. Версионирование IR (Day 1-4)
- [ ] Поддержка миграций (v1.0 → v1.1)
- [ ] Backward compatibility (старые JSON работают)

---

#### 15.2. Чистка и оптимизация (Day 5-10)
- [ ] Удалить дублирующиеся .tsx сцены (оставить только GenericScene)
- [ ] Рефакторинг фабрик (упрощение)
- [ ] Документация (как добавлять новые типы элементов)

---

### 🎯 Итоги Phase 3 (Week 9-16)

**Что получили:**
- ✅ JSON → видео (без написания кода)
- ✅ Backend генерирует сцены динамически
- ✅ Валидация + версионирование IR
- ✅ Один GenericScene вместо 50 .tsx файлов

**Что можешь делать:**
- ✅ Добавлять новые сцены через backend (Java)
- ✅ Менять сцены без перекомпиляции frontend
- ✅ Версионирование (откатываться к старым версиям)

**Готовность к Phase 4:**
- ✅ IR стабилен и версионирован
- ✅ Frontend отделён от логики генерации

---

## 🔴 Phase 4: Production (Weeks 17-24+)

### Цель фазы
Готовая SaaS-платформа с авто-лейаутом, рендер-фермой и UI.

---

### 📅 Week 17-18: Авто-лейаут (Simple Grid)

**Задачи:**

#### 17.1. Grid Layout (Day 1-5)
- [ ] `GridLayoutEngine` (автоматическая сетка 3×3, 4×4)
- [ ] Параметры: `rows`, `cols`, `gap`
- [ ] Тестирование на простых сценах

**До (ручные координаты):**
```json
{ "component": { "type": "TOPIC", "props": { "x": 100, "y": 50 } } }
```

**После (авто-лейаут):**
```json
{
  "component": { "type": "TOPIC", "props": { "name": "events" } },
  "layout": { "mode": "grid", "rows": 2, "cols": 3 }
}
```

---

#### 17.2. Flow Layout (Day 6-10)
- [ ] `FlowLayoutEngine` (автоматический перенос элементов)
- [ ] Параметры: `direction` (horizontal/vertical), `gap`, `wrap`

---

### 📅 Week 19-20: ELKjs Integration

**Задачи:**

#### 19.1. ELK Setup (Day 1-3)
- [ ] Установить `elkjs` (npm)
- [ ] Создать `ELKLayoutEngine`
- [ ] Конвертация Scene IR → ELK Graph

---

#### 19.2. ELK Algorithms (Day 4-7)
- [ ] Layered algorithm (для DAG)
- [ ] Force-directed (для сложных графов)
- [ ] Ортогональные рёбра (UML-стиль)

---

#### 19.3. Тестирование (Day 8-10)
- [ ] Протестировать на 10+ сценах
- [ ] Сравнить: ручные координаты vs ELK
- [ ] Найти edge cases (перекрытия, вылет за границы)

---

### 📅 Week 21-22: Рендер-ферма

**Задачи:**

#### 21.1. Render Queue (Day 1-4)
- [ ] Redis (очередь задач)
- [ ] `RenderJob` (entity)
- [ ] API: POST /render → jobId

**Backend:**
```java
@PostMapping("/render")
public RenderJobResponse submitRenderJob(@RequestBody SceneRequest request) {
    RenderJob job = renderService.enqueue(request);
    return new RenderJobResponse(job.getId(), "PENDING");
}
```

---

#### 21.2. Render Worker (Day 5-8)
- [ ] Node.js worker (Playwright + headless Chrome)
- [ ] Рендер PNG-кадров
- [ ] FFmpeg → MP4
- [ ] Загрузка в S3/MinIO

**Worker:**
```javascript
async function renderJob(jobId) {
  const scene = await fetchScene(jobId);
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  
  await page.goto(`http://motioncanvas:5173/?scene=${scene.id}`);
  await page.click('#render-button');
  await page.waitForSelector('#render-complete');
  
  const videoUrl = await uploadToS3(jobId);
  await notifyBackend(jobId, videoUrl);
}
```

---

#### 21.3. Scene Cache (Day 9-10)
- [ ] Кэш по hash(IR + version + audio)
- [ ] Проверка перед рендером (если кэш есть → возвращаем URL)

---

### 📅 Week 23-24: Web UI

**Задачи:**

#### 23.1. Простой UI (Day 1-5)
- [ ] Textarea (вставить JSON)
- [ ] Кнопка "Render"
- [ ] Прогресс-бар (PENDING → RENDERING → COMPLETE)
- [ ] Предпросмотр (ссылка на MP4)

---

#### 23.2. Шаблоны (Day 6-10)
- [ ] 3 готовых шаблона (Kafka, PostgreSQL, Kubernetes)
- [ ] Кнопка "Use Template" → JSON заполняется автоматически

---

### 🎯 Итоги Phase 4 (Week 17-24)

**Что получили:**
- ✅ Авто-лейаут (Grid, Flow, ELKjs)
- ✅ Рендер-ферма (асинхронный рендер)
- ✅ Scene Cache (ускорение повторных рендеров)
- ✅ Web UI (загрузка JSON, шаблоны)

**Что можешь делать:**
- ✅ Продавать доступ (SaaS)
- ✅ Масштабировать (10-100 воркеров)
- ✅ Не задавать координаты (авто-лейаут)

---

## 🤖 Workflow: Как работать с AI

### Шаг 1: Запрос на компонент
**Ты:**
> "Сделай компонент Redis PubSub: Publisher, Channel, Subscriber. Publisher отправляет сообщение в Channel, Subscriber получает."

**AI (я):**
1. Создаю компоненты (`RedisPublisher.tsx`, `RedisChannel.tsx`, `RedisSubscriber.tsx`)
2. Создаю сцену (`redisPubSubScene.tsx`)
3. Следую архитектурным принципам (AnimatedComponent, Theme, Signals)

---

### Шаг 2: Ты проверяешь
```bash
cd motion-canvas
npm run dev
```

Открываешь `http://localhost:5173/` → выбираешь сцену → смотришь.

---

### Шаг 3: Итерация
**Если не нравится:**
> "Сделай сообщение красным, а канал — зелёным. И добавь анимацию пульсации канала."

**AI:**
Обновляет компоненты, сохраняет архитектуру.

---

### Шаг 4: Рендер
Нажимаешь "Render" в UI → получаешь MP4.

---

### Шаг 5: Использование
Загружаешь MP4 в YouTube, вставляешь в курс, показываешь клиентам.

---

## 📊 Метрики прогресса

### Week 1-4 (Phase 1)
- **Компонентов:** 20+
- **Сцен:** 10+
- **Видео:** 5-10 (для YouTube)

### Week 5-8 (Phase 2)
- **Шотов:** 15+
- **Сцен (shot-based):** 15+
- **Видео:** 10-15

### Week 9-16 (Phase 3)
- **JSON-driven сцен:** 20+
- **Backend эндпоинтов:** 10+
- **Видео:** 20+

### Week 17-24 (Phase 4)
- **Авто-лейаут:** 3 алгоритма (Grid, Flow, ELK)
- **Рендер-ферма:** 1+ воркеров
- **Web UI:** Готов
- **Монетизация:** Freemium запущен

---

## 🚨 Риски и митигации

### Риск 1: Burnout (выгорание)
**Митигация:**
- Маленькие шаги (1-2 компонента в день)
- Не пытаться сделать всё за 2 недели
- Отдых каждую неделю (1-2 дня без кода)

---

### Риск 2: Scope creep (раздувание скоупа)
**Митигация:**
- Чёткие границы фаз (не делаем Phase 4 в Phase 1)
- AI следит за соблюдением архитектуры
- Регулярные ревью ("что реально нужно?")

---

### Риск 3: Авто-лейаут не взлетит
**Митигация:**
- Начинаем с ручных координат (Phase 1-3)
- Grid layout (Phase 4, Week 17-18) — простой fallback
- ELKjs (Week 19-20) — только если Grid недостаточно

---

### Риск 4: Перфекционизм ("не идеально")
**Митигация:**
- MVP > Perfect
- Лучше 10 "нормальных" компонентов, чем 2 "идеальных"
- Рефакторинг потом (когда будут реальные пользователи)

---

## 🎯 Финальный чек-лист (Phase 1)

### Week 1
- [ ] AnimatedComponent + Theme
- [ ] 5 Kafka-компонентов
- [ ] 1 Kafka-сцена → MP4

### Week 2
- [ ] 5 DB-компонентов
- [ ] 3 DB-сцены → MP4

### Week 3
- [ ] 5 Network-компонентов
- [ ] 5 Algorithm-компонентов
- [ ] 3 сцены → MP4

### Week 4
- [ ] 4 K8s-компонента
- [ ] Изометрия (RendererIso)
- [ ] 2 изометрические сцены → MP4

**После Week 4:**
- ✅ 20+ компонентов
- ✅ 10+ сцен
- ✅ 10+ готовых видео для YouTube

---

## 🚀 Следующий шаг

**Ты готов начать Week 1?**

Скажи:
> "Начинаем Week 1. Сделай AnimatedComponent + Theme."

Или:
> "Сделай сразу Kafka-компоненты (Producer, Topic, Consumer)."

**Я жду твоей команды!** 🎬



